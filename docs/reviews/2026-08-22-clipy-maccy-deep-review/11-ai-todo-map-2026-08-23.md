# AI 复审 TODO Map（2026-08-23 · 晚间全量差距版）

> **本文件由 AI 生成**：第一轮由 ZCode agent（模型 GLM-5.3，builtin:bigmodel-coding-plan）
> 于 2026-08-23 09:47–10:10 +0800 生成；**本版为第二轮全量更新**，由 Claude Code
> agent（模型 GLM-5.3）于 2026-08-23 18:30–20:45 +0800 基于 13 个并行读手 +
> 2 个对抗验证器的差距盘点 workflow 重写（方法与证据上限见 §9）。它是一次性的
> **时点审计 + 待办地图**，不是活文档。
> [`10-implementation-status.md`](10-implementation-status.md) 仍是 REVIEW 中唯一活实现账本，
> owning spec（`docs/`、`docs/v2/`）仍高于本文件；两者冲突时以账本与 spec 为准，并请以
> 下一个实现 PR 修正本文件的过时行或将其归档。
>
> **快照基线（本版）**：master `53c347d`（PR #30 合并，2026-08-23）。当日事件：
> PR #24–#30 落地，两个 dispatch-only 物理/跨进程证据 cell 转绿（§1）；账本 §7/§20、
> instrumentation 行与 `docs/PROGRESS.md` Batch 19 已同步。
> 第一轮基线为 `cda2ba0` → `a3e6774`（PR #16）；第一轮 §1 的 PR #2–#16 绿色 run 表
> 仍有效，不再重抄。

---

## 0. 本版更新摘要

- §2 按 master 现状复核第一轮发现：2.1（X.7 五处"未合并"文字漂移）与 dwell 测试
  真实 sleep（LEGACY-S-8）**已随后续 PR 解决**；2.2/2.3/2.4 大部分仍开放。
- §3 状态总览刷新至 PR #30 后。
- §4 重写为**全量差距盘点**：11 个 canonical 面积、~134 条聚合缺口（当前-lane
  acceptance ~111 + future-work ~16 + design-only ~7 聚合），逐条给出 ID、差什么、
  怎么关。取代第一轮 §4 的 区 0/A–L 框架（旧区字母在表中以〔区 X〕标注对应）。
- 新增 §5 文档-账本冲突复核记录、§6 今日已缩小清单、§7 更新的领取顺序建议。

## 1. CI 真相基线（gh CLI 核实，2026-08-23 20:00 前后 +0800）

- **两个 dispatch-only 证据 cell 当日转绿**：
  - General pasteboard 跨进程可见性：run
    [32632263996](https://github.com/GuangDai/Clipy/actions/runs/32632263996)（首次绿）。
  - Card 6B APFS ENOSPC capture 事务叶：run
    [32636093920](https://github.com/GuangDai/Clipy/actions/runs/32636093920)（证据行
    `pressure_capture=transaction rejected with seed preserved`、
    `fresh_verify=seed state readable`）。
- master correctness 绿：PR #30 合并后 push run
  [32636602299](https://github.com/GuangDai/Clipy/actions/runs/32636602299) 三 job 全绿
  （完整重跑；其首次 attempt 与 e915848 的 push run 均因 `HistoryViewStateTests`
  pollUntil 簇超时而红——同 commit 重跑即绿，判为 runner 慢窗抖动，见 §2.3）。
- 当日 PR 链（全部 PR-run 三 job 绿后 merge）：#24（修复两处字符串插值语法错误 +
  NSError 反射断言）、#25（docs/05 §16 stamped-plan capacity admission）、#26（raw
  `volumeAvailableCapacity` 事实）、#27–#29（APFS EXIT trap 子 shell 触发三连修）、
  #30（账本/文档同步）。
- X.8/F0A/Batch 18–21 链（PR #17–#23）见账本 §16–§21，本版不重抄。

## 2. 本轮审计发现（两轮合并，按 master `53c347d` 复核）

### 2.1 已解决（第一轮发现，现核实关闭）

- **TODO-DOC-1（X.7 文字漂移）**：五处 "unmerged/未合并" 表述已随后续 PR 消失
  （本轮全库 grep "unmerged" 零命中）。
- **LEGACY-S-8（dwell 测试真实 sleep）**：`PreviewPaneStateTests` 已改用有限
  scheduler-turn oracle（Batch 19 tests-only 改动），`Task.sleep` 零命中。验收器对
  第一版盘点"仍列为缺口"的质疑成立，本版从缺口清单移除。

### 2.2 仍开放（第一轮发现，本轮 grep 复核仍在）

- **UI-10 文字面**：[`FloatingPanel.swift`](../../../ClipyApp/Sources/Panel/FloatingPanel.swift)
  注释 "every space" vs `.moveToActiveSpace` 的措辞与三处文档不一致（Apple 文档判据
  见 §8）；真机 Space/Stage Manager/全屏矩阵未跑（归 §4 面积 3/4）。
- **TODO-X7-1（X.7 实体保真）**：`ClipboardIntentModels.swift` 仍硬编码
  `title = "Clipboard Item"`（L86）与 `revisionCount = 0`（L70/L81）——BLOCKED-SPEC
  （须先扩 HistoryCore 读 DTO 并过 symbol snapshot）。
- **APL-C-05**：`PasteboardMarkers.swift:20` 仍称 "the three NSPasteboard framework
  markers"；应改为第三方尽力 denylist 措辞 + 真实密码管理器矩阵（归 §4 面积 5）。
- **GOV-3 残余**：Card 5D 只收窄了两个 pasteboard knob；完整 Release/signed symbol
  与其余 test instrumentation 审计仍 Open（归 §4 面积 7）。
- **Card 10 Settings**：`ClipySettingsView` 仍是无 per-field dirty bits 的
  `status = .success("Done.")` 模式（×4 处）；10A–10E 六 leaf 全 Open（10A 前置
  GOV-2 的 `DEC-RET-READ` 裁决）。

### 2.3 当日新事实登记（细节以账本/AUDIT/PR 为准，此处只登记）

- **Core Data external-storage 满盘异常**：run 32632262141 证明满盘 + 大 blob 时
  Core Data 抛未捕获 `NSInternalInconsistencyException`（"Can't create
  externalDataReference interim file : 28"）→ SIGABRT，而非 NSError——`Persistence
  ErrorClassification` 根本没机会跑。产物：docs/05 §16 stamped-plan capacity
  admission（PR #25）+ `CaptureCapacityAdmissionTests`。**admission 通过后的中途
  耗尽是已记录接受的 Apple framework crash ceiling**（AUDIT §3），不是可领工作。
- **容量事实键反例**：`volumeAvailableCapacityForImportantUsage` 在挂载卷返回 0
  （run 32634051113；purgeable 会计只在启动卷维护，SO 56474397 同案）→ admission
  改用 raw `volumeAvailableCapacity`（PR #26）。
- **bash 3.2 子 shell EXIT trap**：macOS runner `/bin/bash` 3.2 在替换子 shell
  中途触发继承的 EXIT trap（runs 32634454727/32635233048/32635568571 三次实证；
  `$BASH_SUBSHELL` 不递增、无 BASHPID）→ APFS 脚本 EXIT trap 定性为
  breadcrumbs-only（PR #29）；**勿在任何 CI 脚本的 trap 中 reintroduce 资源 teardown**。
- **`HistoryViewStateTests` rerun-flake**：两次 push run（32636092247、PR #30 CI）
  在 ~11:20–11:26 UTC 窗口成簇 pollUntil 超时，同 commit `gh run rerun --failed`
  即绿。处置约定：先重跑再诊断。
- **Swift 单行字符串插值不可含换行**（PR #24 两处）；ObjC 类 metatype 反射不带
  模块前缀（`swift_type="NSError"`，非 `Foundation.NSError`）。

## 3. 状态总览（review 判定 → 当前，PR #30 后）

- **P0-0（红色 SHA）**：已修复（PR #2–#16）。
- **正常路径 correctness**：model/source 层大体闭合（batch 2–5、18）；**hosted 真实
  控件/AX/键盘 journey 层大面积未闭合**；Batch 21 五叶 In-progress 待 merge+CI。
- **签名隐私/capture（CLIP-1/5A/6B）**：Card 6B 的真实满盘 capture 叶**当日转绿**；
  5A 的 signed 四态矩阵、TCC、open/migration/revise 满盘仍开。
- **物理/跨进程证据**：pasteboard 跨进程与 APFS ENOSPC capture 两格绿；**其余
  runtime/发行证据格基本全开**。
- **资源界（Card 11/12/13、STOR/MEM）**：11A admission 叶 In-progress；除
  `PLAY-STOR-1`、`PLAY-TIER-2A-THUMB` 外全部 Open（dedup 仍全量 hydrate、
  ThumbnailStore 无 permit）。
- **Gateway/Python（X 系）**：X.2–X.8 上限内 Done；**X.9+/clipyctl/transport/
  authentication 未动**。
- **Formats/Preview**：仅 `ClipboardFormats` stable facts + TYPE-2 stopgap；整区
  ~90% 未动。
- **Tier/U-scale/P3**：characterization 可领；TIER-2B+ 全部 BLOCKED-SPEC/G8；
  `DEC-U-SCALE-STARTUP-INDEX` 等 36 OPEN + 2 BLOCKED。
- **State 3**：除 `SIGNED-RUNTIME-0` 有界 lane 外全部 Open；产品不可分发。

## 4. 全量差距盘点（11 面积 · ~134 聚合项）

> 层级：**acc** = 当前-lane 验收叶（可领）；**fut** = future-work（被评审自己的
> approval/evidence 门挡住，不可当当前 lane 领取）；**des** = design-only（须 spec/
> ADR 决策先行）。状态来源：账本行号（L…）或"无行"（评审明文要求但账本无行，
> 领取前须回 04 铸叶或由实现 PR 补行，§23 规则 5）。★ = blocking 后续 lane 的
> 验收格。完成度为粗估（按已关 leaf vs 本表聚合项口径）。

### 4.1 正常路径 correctness 遗留（16 项 · ~65%）〔区 A/B〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ★ Batch 21 五叶 | acc | 8B hosted 全置顶分页、9C hosted 面板 frame、11A search admission、unified retention draft、15D 捕获失败播报：source 已在 branch，缺 merge + macOS CI | merge 后各仍有 ceiling 尾巴（见下行起） |
| Card 3B（L53） | acc | 真实 stale alert、Reload control、失败恢复 hosted journey | 真实 editor seam 红→绿 + CI |
| Card 3C（L54） | acc | 真实 dirty Cancel/Esc 确认 journey（含 Esc） | hosted 红→绿 |
| Card 3D（账本 §16） | acc | **Save 追加不可变 revision 的披露文案决策 + hosted copy test**（注意：不是 9D retry——那是 Card 15 键盘恢复格） | 文案决策入 spec + hosted test |
| Card 9D/15 键盘恢复（L338） | acc | 真实 Retry/no-Retry control 与键盘可达 journey | hosted journey 绿 |
| Card 8C（无行） | acc | 有 next 时显示 `50+`/`50 shown`；authoritative 删除 selected 后 selection 清空、action disabled | 铸叶 + 红→绿 |
| Card 8G（L63/L87） | acc | AX-exposed Clear control + autocorrection-off 真实验证 | hosted/AX journey |
| Card 9C/9F 尾巴（L66/L315） | acc | 位置按 stable main-content rect 保存；无不可达 721pt 窗口 | merge+CI 后收口 |
| Card 11A 尾巴（L316） | acc | 绿后加 poison-probe 证 0 corpus 触达；PERF-1 empty fast-path 与协作取消后续无行 | 铸叶 |
| unified retention 尾巴（L317） | acc | localization、完整 hosted Settings journey；~~receipt/purge external ingress~~ 已裁决不存在（47-4：V2-05 §2.2/§3.2、V2-02 §9——Settings经v1 seam是唯一owner；防回归：`ExternalRetentionIsolationTests` + `ExternalAccessPolicyTests` 19-case词汇天花板） | 后续 hosted journey |
| Card 10A–10E（无行；代码核实） | acc | Settings per-field dirty bits、保存确认、launch-at-login 完整四态 UI（`AppDelegate` 现压平成 Bool） | 铸叶；10A 前置 `DEC-RET-READ` |
| ★ Card 9B external ingress（L105/§22） | acc | App Intents/Gateway remove 绕过 `HistoryViewState`；observe 无 off-page preview/thumbnail/details purge facts | external ingress 接同一 surface-invalidation owner + purge facts |
| ~~Card 11C/PERF-3/PERF-2~~（无行） | — | **已解决**（47-2 裁决：grammar 冻结不变 + 扫描换 `enumerateMatches` 可中断迭代器 + 2,000ms typed `.searchEngineDeadline`；PERF-2 的取消面由 11B 的 32-row checkpoint 与 47-2 的引擎内取消共同闭合；判别证据 run 32757784066/33177236846） | — |
| Card 2B-2（无行） | acc | Storage ID-source 碰撞恢复：先批 retry bound 与耗尽 typed failure，再固定 collision ID source 经真实 History 写 Red | 决策 + 红→绿 |
| UI-2（无行） | acc | Details Pin/Remove 的 await-receipt 写后读排序（评审要求；现状为 fire-and-forget + 独立 reload） | awaitable receipts + happens-before 测试（须先铸叶） |
| DATA-5a / DATA-10 / CLIP-8 / CLIP-9（无行） | acc | 四项**语义裁决**：retention 时间权威（admitted-time vs observedAt 偏斜）、revert/unpin 双裁决、启动导入 vs baseline（`DEC-OBSERVER-START`）、粘贴选择语义（`DEC-PASTE-REFERENCE`，决策非 bug） | owning-spec 各记 chosen/rejected + 判别测试 |
| ~~LEGACY-S-8~~ | — | **已解决**（Batch 19 有限 scheduler-turn oracle；§2.1） | — |

### 4.2 External Gateway X.2–X.8 后续（7 项 · ~50%）〔区 I〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ★ F1 client credential custody（L294） | acc | client 侧 owner-only no-follow 文件（dir 0700/file 0600）、继承 stdin 供应、authority-last/first 发布次序、crash-orphan 清理 | exact file 测试 + orphan fixtures |
| ★ Authentication + AuthenticatedIngressFacade（L294） | acc | credential 比对与受限 ingress facade（`DEC-PY-AUTHENTICATED-INGRESS` 命名 blocker） | 薄 facade 委托唯一 ExternalGateway + kind-mismatch 拒绝测试 |
| Opaque locator/cursor（07 L435-441/L832） | acc | Gateway 铸造 locator/cursor 未实现；X.8 wire 上限 4,096B vs `HistoryPageCursor` ~26KiB + locator lifetime 未冻结（挡正向 B4/B5） | spec 裁决（bounded server-side state 或 versioned encoding）+ C2–C5 Reds（禁 hash 捷径） |
| X.9 只读 positive browse（§22 L347-350） | acc | 经真实 adapter 的本地 enroll/deny/grant/revoke/browse 闭环 | F1 卡闭合后接 |
| Signed DPK Keychain 往返（L294 ceiling） | acc | signed artifact 的 DPK persistence/reopen/delete、access-group/profile、client 可读性（现测试不穿真 Keychain） | Developer ID/profile 矩阵取证 |
| X.7 signed-runtime cells（L263/L340-346） | acc | framework system-manager resolution、真 Siri/Shortcuts 冷/热 invocation、discovery、process placement、Swift 6 queue-crash、TCC | signed 矩阵逐格 |
| Audit crash-window（07 §8.1/§14） | acc | crash-after-audit-before-return 窗口与 append-failure 不释放 DTO 的崩溃证据 | 崩溃/注入窗口叶 |

### 4.3 物理·运行时·发行证据（14 项 · ~20%）〔区 C/G/H〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| Card 6B open/migration 满盘（L138/§22） | acc | disk-full 下 store open 与 V1→V2 migration 的 typed old-or-new 未证 | disposable volume 格 + fresh-child reopen |
| Card 6B revise/remove/clear 满盘（§22） | acc | 写路径满盘 typed 拒绝未证（remove/clear 本身要写盘，不能承诺释放空间；mid-transaction 耗尽已是接受的 crash ceiling，不计可领） | 同 lane 写路径格 |
| Card 6B 发行身份环境恢复（§22） | acc | Developer ID 环境满盘恢复 | 恢复 cell |
| ★ Card 5A signed 四态矩阵（L298/§22） | acc | 真实 prompt、TCC/System Settings 切换、clean profile、跨 restart/login 的 AccessBehavior 四态（当日跨进程格明确不含 TCC） | signed clean-profile 矩阵 |
| Carbon/快捷键/Secure Input 矩阵（无行） | acc | 真 Carbon 回调线程/布局/冲突、Secure Input 无 runtime 证据 | macOS 26 真机矩阵（05-EV2/OQ5） |
| ★ 真实 XCUI journey tracer（无行） | acc | 仓库无 UI-testing target；`UISmokeJourneyTests` 直调 state 证明不了 status item/panel/first responder/剪贴板往返 | XCUI summon→search→Return→clipboard→close 红→绿（REL-2/05-CE21） |
| StoreRoot + 分类恢复 UX（§22） | acc | 专用 StoreRoot、`ModelContainer` 错误分类（corruption/permission/ENOSPC/future-schema）、Retry/Reveal、用户确认隔离（现 `.openStore` 一刀切+Quit） | DATA-14/05-OQ13-15 |
| 子进程 kill/reopen 链（无行） | acc | WS14 writer→reader、migration seed→migrate→kill→reopen、普通事务 child-kill/external-blob-reopen 矩阵 | DATA-13/05-CE23/26/REL-4 分段 tracer |
| External-clone 验证子进程（无行） | acc | CI 白名单缺独立 validation child：seed 退出后全量 hydrate+digest 对 manifest 验证 | terminated seed + fresh validator |
| Card 10C 运行时格（L300） | acc | fresh install、external revoke、logout/login、真 System Settings、signed installed runtime | 签名安装态矩阵 |
| Card 5D instrumentation audit + Developer ID（L327-329） | acc | 26-literal 负匹配只对 ad-hoc artifact 成立；完整 audit 与 Developer ID 证据 | Developer ID 全量 audit |
| Card 6 banner 物化尾巴（L95 ceiling） | acc | capture-health episode/notice owner 已 Done，但 banner/dismiss control 实际 materialize 未证 | hosted UI 物化证据（验证器 A 补） |
| Retention config singleton 重启尾巴（L89 ceiling） | acc | existing-value validation 已 Done，但 true restart 与 migration 矩阵未证 | 重启/migration 格（验证器 A 补） |
| Panel Space/StageManager/多屏/FUS 矩阵（无行） | acc | `.moveToActiveSpace` 等行为旗标无真机矩阵，面板最小行为未选定 | 真机矩阵 + 行为选定（UI-10/05-EV4/OQ7） |
| 同机 A/B 三旅程（00 §6.10） | fut | 优越性证据 NONE；评审明排 LAST | 签名产物就绪后三旅程 |

### 4.4 Panel·UI·AX·localization（9 项 · ~40%）〔区 A/F/G〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| Card 15D 尾巴（L318） | acc | 绿后仍只证 content-free notification seam——VoiceOver 实际朗读、AX tree、FKA、localization | 真 VO 读出证据 |
| Row AX actions（L93） | acc | per-ID AX identity/default Copy/具名 action 只有 source wiring；runtime handler、FKA、label 质量 | hosted NSAccessibility 树验证 |
| ★ UI-16 VO 可操作契约（§22） | acc | release blocker：默认动作（Copy）、图像预览语义 label、settled generation 才播报 | NSAccessibility 树 + Apple VO 判据 + 真实 journey |
| Card 14 四 decision gate（04 L813-815） | acc | open 时 selection/focus、close 后 state、快捷键冲突 warn vs hard reject、session/sleep 时 capture 暂停 | 决策 + 对应 Reds |
| Card 14A/14C/14D（04） | acc | panel-session reducer（含 IME Esc/Return 委托）、reopen/resign/sleep/wake/screen-change 生命周期、唯一 lifecycle owner hosted 证明 | 铸叶逐张 |
| 自定义快捷键 + 冲突恢复（§22） | acc | 可配置、冲突可见、失败保留旧绑定 + 恢复 UX（现固定 ⇧⌘C/⌃Space 冲突且注册失败被忽略） | UI-8/§4.7c + signed 布局矩阵 |
| Localization 基建（§22） | acc | String Catalog/FormatStyle/plurals 未接入；伪本地化/RTL 未证 | UI-17/P6-93a |
| UI-7 键盘首路径契约（无行） | acc | summon 选最新/输入即搜索/方向键/Return/Esc 契约与 CJK marked-text IME 优先未定义测试；FocusState 未设 | 契约文档 + 含 IME 测试 |
| TODO-X7-1（§2.2） | acc | X.7 实体 title/revisionCount 保真（BLOCKED-SPEC：须扩 HistoryCore 读 DTO） | spec + symbol snapshot |

### 4.5 隐私·安全·pasteboard 语义（9 项 · ~40%）〔区 C〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| Card 5A Pause UI（L298） | acc | readFailure/userPaused 只是 reducer 状态，无可见限时 Pause/恢复入口 | P2-53a 明显 Pause/Resume |
| CLIP-1 deny≠空历史（L298） | acc | deny/pause 不得渲染成"无捕获值"空历史的区分性 UI 与解释面 | denied 独立呈现 + 恢复入口 |
| DEC-CAPTURE-OVERLOAD 判别实验（无行） | acc | 过载政策（bounded FIFO vs active+latest vs 显式拒绝）未经 queue-budget 判别实验入 spec | 05-DE1/CE3 + stress |
| DATA-7a 单写者租约（无行） | acc | store URL symlink/别名规范化、单实例声明、StoreRoot 进程级窄租约 | child 测试：别名收敛/第二租约失败 |
| DATA-9a volatile-UTI fixtures（无行） | acc | Chrome/Safari/Notes volatile bookkeeping UTI 是否致同文案重复项无 fixture 证据 | 合成/授权 fixture 测频 |
| APL-C-05 措辞 + 密码管理器矩阵（无行） | acc | marker 六字符串应改"第三方尽力 denylist"措辞；真实矩阵未跑 | 措辞改写 + 矩阵 |
| DATA-0 entitlement 负门（部分无行） | acc | "任何 iCloud entitlement 即红"的 CI manifest 级门无行（现只有 ad-hoc lane 负门 + 源码 `.none`） | manifest 负门入常规 CI |
| CLIP-7b / DEC-MULTI-ITEM | fut | 多项值图整链（顺序入 equality/fingerprint 等） | 用户任务实验达阈值后设计 |
| DATA-9b 角色拆分 | fut | "可粘贴但去重忽略"的表示角色拆分 | 9a 证据确认后判别测试 |

### 4.6 证据协议与开放问题（13 项 · ~40%）〔区 H/GOV〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ★ GOV-1 残留（无行） | acc | 被跳过的 Exact matcher A/B 与 5k evidence job（run 96368136670/96368137104 skipped）未在绿 SHA 执行记录 | 当前绿 SHA dispatch 两 job 并记录 |
| GOV-4 symbol snapshot 深锁（无行） | acc | 只锁标题集；overload/参数/返回/async-throws/isolation/declaration-fragment 未锁；无外部 client 编译 fixture | 扩展生成器 + drift 自测入 gate |
| GOV-4 三 gate（无行） | acc | escape_hatch 扩 ClipyApp/Tests + 自测、import 依赖图门、"只有 HistoryAuthority 建 writable ModelContext"所有权门 | 三 gate 入常规 CI |
| GOV-4/05-FC9 分类器对抗 fixtures | acc | CoreData 分类器脚本化 + 白名单不吞交错错误的对抗 fixture | 抽脚本 + fixtures 入 gate |
| 05-FC7 RSS gate 正值（无行） | acc | 只查 `.time` 非空，unavailable 可绿 | 缺失即失败 + 单位 |
| 05-FC10 symbol-bot 触发（无行） | acc | bot push 不触发 main CI；账本可引 pre-bot SHA | bot push 触发 + 引用含 bot run |
| Card 17 gate 6/7/9（04） | acc | XcodeGen output 契约门、functional/perf 非零执行守卫、XcodeGen 固定版本+双生成 diff | 各一负例 fixture + 最小 Green |
| PERF-9/05-FC8/FC4 诚实性能主张（无行） | acc | O(NlogN) 重标、exact admission p95/p99 从过时 125s 预算解冻、render-speed 指标改名/补真实帧 SLO | 重标 + 恢复 p95/p99 |
| 05-CE22 WS13 zero-publish 计数（L92 未含） | acc | non-coalescing publish-counter 断言未列上限 | 补计数器/非合并探针 |
| SLO-1/SLO-2 指标集冻结（无行） | acc | launch-to-ready/summon-to-row/keystroke-stable/selection-to-preview/Return-to-receipt/RSS 未冻结、未在 arm64 基线 | 冻结 + 基线后批阈值 |
| 05-ST1..6 统计纪律（无行） | acc | 样本/百分位/超时计失败/冷热分开/比例覆盖尺度/无复合分未在 runner 强制 | perf runner 报告规则 |
| 05 §7 33 项 Apple 开放问题 | acc | AccessBehavior 矩阵、NSPasteboard 线程安全、lazy provider 时延、writeObjects 竞争、SwiftData 迁移 kill 原子性、.externalStorage 时机、批量 fetch RSS 等 | 逐问 characterization cell 或保守措辞 |
| 03 §13 DEC 注册表 36 OPEN + 2 BLOCKED | des | `DEC-RET-READ`/`DEC-PREVIEW-TARGET`/`DEC-THUMB-CACHE`/`DEC-CAPTURE-OVERLOAD`/`DEC-CAPTURE-CLOCK`/`DEC-RET-AGE`/`DEC-REVERT-RACE`/`DEC-UNPIN-SWEEP`/`DEC-DISTRIBUTION` 等 | 逐行决策记录（4 行允许 test-only 先行） |

### 4.7 架构深化（7 项 · ~50%）〔GOV-2/3〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ★ GOV-2a / P0-0.3 `DEC-RET-READ`（无处置行） | acc | retentionConfiguration() readback 的 public seam vs 内部 UI seam 裁决——V2-02 仍 write-only 而 unified-retention 叶已在消费该 seam | V2-02 修订（或撤 API）+ snapshot/账本同步 |
| ★ GOV-2b / P0-0.4 `DEC-PREVIEW-TARGET`（无处置行） | acc | PresentationUI 的 ImageIO import 与 public CGImage 面（`ThumbnailStore.image(for:)`、`DisplayImageDecoder` 跨 actor 返 CGImage?）未裁决/修复 | V2-07 修订或解码移 Storage/新 owner；neutral bounded raster artifact |
| DEC-THUMB-CACHE（无行） | acc | process-lifetime completed cache 是未准入机制：G1 复用/延迟/RSS 实验未跑 | 05-DE4 60s 真实滚动测量后裁决 |
| GOV-3 其余 knob 收缩 + surface gates | acc | ThumbnailStore 公开 knobs/counters、`PreviewContent.resolve`、caller-constructible `CaptureOutcome.init`、PasteboardAdapter/PresentationUI 最小 public 面 | @testable/package-internal 注入 + 两 gate |
| CaptureLane 提取 | fut | 删除测试证明 locality 改善才提取 | 证据先行 |
| ContentEditing 抽取 | fut | 出现第二个非 UI caller（Python edit）才深化 | 触发条件先行 |
| 05-ART12/DL17 序列化纪律（无行） | acc | "artifact 只出自生产 owner 稳定 facts + 独立 pure serializer、测试不复制 UTI 表/路由"未制度化审计 | seam/inventory 审计入评审 gate |

### 4.8 Python 本地自动化（18 项 · ~15%）〔区 I〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ★ clipyctl executable + process I/O（§22） | acc | X.8 只是 package-only pure codec；真实 FD/launch/ready/发现规则未交付 | 07 §2.1 CLI 骨架 + 发现规则冻结 |
| ★ DEC-PY-TRANSPORT（03 L810） | acc | UDS/Apple Events/App Intents/XPC+签名桥 的单一生产 transport 未选（F0A 只证 ad-hoc 同 EUID UDS 机制） | 07 §6 矩阵判定唯一 transport |
| PY-4 / B4/B5 signed 冷启 browse tracer（07 L671-679） | acc | 全退出 signed app + 仅 browsePreview 授权 + Python 拉起真 clipyctl→ready→三 locator 垂直 tracer | X.9 承接 |
| PY-5 browse 即敏感（07 L682-685） | acc | 合成 secret：无授权不出现在 response/audit/stderr；有授权只在 stdout 且 audit content-free | synthetic-secret grant 测试 |
| PY-7/8 Effective-only 有界二进制读（07 L694-707） | acc | 只返当前 Effective（禁 Canonical/修订/lineage）；cap±1/NUL/半帧/慢读/断开/继承 FD 逐卡 | PLAY-PY-D1A…D8 |
| PY-9A/9B organize + deleteItem（07 L710-716） | acc | 仅 pin/unpin 的 organize 与独立破坏性 grant 的 delete；确认政策 `DEC-PY-DELETE-UX` 未裁 | E1A/E1B；先裁 E2 |
| PLAY-PY-E5R/E5W（revoke race） | acc | park read 在 fast precheck 后、authoritative release 前 revoke 必须零输出；park write 在 commit 前 revoke 必须零写 | 各自独立 Red |
| PLAY-PY-E6A/E6B（timeout/no-blind-retry） | acc | CLI timeout 默认不 blind retry、返回 typed transient | 各自独立 Red |
| PY-13 同 UID 安全矩阵（07 L742-746） | acc | 同 UID Python/异 UID helper/错凭据/撤销后旧凭据：四负一格正 | PLAY-PY-B3A-C + B4；F3C |
| PY-15 四格 signed 发布矩阵（07 L754-759） | acc | 签名链/生命周期/调用方（Terminal·IDE·venv·launchd）/沙箱-TCC | F3A-D + aggregate |
| PLAY-PY-C1 纯投影 golden（无行） | acc | capability summary 纯投影/序列化 golden（可立即做） | golden fixtures |
| 05-ART1 能力快照（无行） | acc | LocalAutomationCapabilitySnapshot（build/沙箱/entitlement/transport/冷热/入口/peer 身份/typed failure） | 签名构建逐格取证 |
| reviseContent PY-12A/B | fut | 独立高危 grant + OCC + stale/fresh（评审排最后） | D9 后 E3/E4；两 DEC 先裁 |
| E7 持久幂等 + F2 + 订阅 | fut | `DEC-PY-IDEMPOTENCY` OPEN；F2 仅当需第二 adapter；订阅需溢出/重连/撤销语义 | 各 spec 批准后铸叶 |
| describeFormatCapabilities 端点 | fut | 被 `DEC-FORMAT-INVENTORY-OWNER`（BLOCKED-SPEC）挡 | 裁决后 PLAY-FORMAT-G |
| §5.2 沙箱/恶意同 UID 监护 | fut | 抵抗恶意同 EUID 或 App Sandbox 需新监护模型 | 触发时新决策 |

### 4.9 内容类型与预览（24 项 · ~10%）〔区 J〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| Owner manifests + CapabilityInventory 九检查（§22） | acc | Search/Thumbnail/Preview/Edit/Pasteboard 各自 manifest 与 build/test inventory join（drift 门、HEIF/BMP 图标差、双 concealment 集共享） | TYPE-1/FORMAT-1D ratchet |
| ★ ContentPreview target + 四 spec 工件（08 L435-455） | acc | package-only ContentPreview target 未建；01-architecture/Package.swift/.swiftlint.yml/import gate 四工件先改 | CONTENTPREVIEW-SPEC-CHANGE 批准后落 |
| PREVIEW-1 loader→render seam tracer（无行） | acc | 慢图→快切文本只发布后者、active jobs/保留字节回落、panel 关后无迟到发布 | PLAY-PREVIEW-0/A1-A5 |
| DisplayImageDecoder CGImage 跨 actor（无行） | acc | actor 向 MainActor 返 CGImage? 违反 repo 规则，靠注释例外 | 与 GOV-2b 一并裁决换 artifact |
| 预览零外联 I/O 默认 + canary（无行） | acc | 默认 hover/dwell/缩略图零 DNS/HTTP/外文件/iCloud/promise 兑现/自动播放 + 临时文件清理 | ZERO-IO-DEFAULTS canary 全零 |
| FORMAT-2A HEIF/BMP 行图标 family（无行） | acc | public.heif/com.microsoft.bmp 可进缩略图/详情/预览但 HistoryRowView 图标缺 | family facts + 映射迁移 |
| FORMAT-2B 运行时准入交集 + PreviewCapabilitySnapshot（无行） | acc | 声明路由 ∩ 运行时能力 ∩ 实际解码 三元交集与 05-ART4 快照未建 | 单一 exact fixture + per-build 快照入 CI |
| FORMAT-3 unknown-UTI 往返门（无行） | acc | 合格未知/dyn.* UTI 的 raw 捕获/存储/逐字粘贴/无语义预览回归门 | PLAY-FORMAT-A1 三 fixture |
| UTF-16 字面量 + 表面分歧（无行） | acc | 硬编码 `public.utf8-external-plain-text` 是错误 Apple 字面量；Projector/大预览可解 UTF-16 而 Details/Revise 强制 UTF-8 分歧 | facts 改正确字面量 + 分歧 manifest 化 |
| PREVIEW-RTF/PDF/HTML/FILE 四卡（无行） | acc | RTF 惰性链接+附件预算、PDF 有界页、HTML 离线纯文本优先、file-URL 元数据优先+两步显式加载 | 逐格式铸叶 |
| PERF-8 尺寸契约 + 回退裁决（无行） | acc | max(w,h) 致另一轴超界；候选回退须先批（05 §14.5 现 fail-closed） | 双轴 aspect-fit 或改标量契约；DEC 先裁 |
| PERF-10 垃圾字节分类（无行） | acc | junk 字节声明 public.png 现报 `.corruptStoredValue`，误报存储损坏并滚动重解码 | fake-PNG fixture 锁定重分类（或保留 fail-closed+文案） |
| PERF-5 离主解码实证（无行） | acc | `kCGImageSourceShouldCacheImmediately` 默认 false——首帧可能 MainActor 解码；无心跳/Profiler 证据 | eager 物化 + 首绘/主线程心跳 |
| PERF-6 预览热路径（无行） | acc | 200ms dwell 触发 details 全量解码 canonical+修订且 Details 解码两次；dwell 政策未入产线 | bounded 前缀 + 非 main 解码一次 |
| 05-ART5 pasteboard 互操作 fixtures（无行） | acc | 多 item/重复 UTI/未知·动态·私有/空字节/延迟 provider/owner 变更 + 对象级读回 | fixture 集；真 App 矩阵其后 |
| 05-ART6 解码器语义 fixtures（无行） | acc | exact UTF-8/16、RTF/RTFD 附件、PNG/JPEG/HEIF/GIF/TIFF、PDF 各态、type-label mismatch | 逐族 fixture |
| 05-ART7 解码器资源包络（无行） | acc | 巨尺寸/多帧/深元数据/截断/高压缩 的子进程 RSS/CPU/墙钟/像素/崩溃包络 | 逐格式子进程包络 |
| STEP-9 生产者/消费者矩阵（无行） | acc | TextEdit/Safari/Finder/截图/Preview/Photos/GIF/TIFF/PDF/自定义 provider 案例记录 | 逐案例入册 |
| HEIC/GIF/TIFF 逐格式 + UTF-16 编辑 + RTF 抽取 | fut | 各需 fixture/包络/错误映射/a11y | 08 §13 顺序 |
| WebKit/QuickLook/AV/URL·color·promise 路由 | fut | 各自 spec/证明门后启用 | 默认路径永不下放 |
| DEC-FORMAT-INVENTORY-OWNER + TIERS + 八维 | des | 能力投影 owner 依赖图/注入生命周期（BLOCKED-SPEC）、首版格式/包络阈值、八维模型 | spec/ADR 冻结 |
| MULTI-ITEM 六项 spec 裁决 | des | item 顺序入 equality/fingerprint、声明顺序持久 vs 排序、marker 拒整快照 vs 单项、lineage 多项写回、部分物化整败 vs 逐项、修订粒度 | 六项入 spec 后开 PB-MULTI-1B |
| CONTENTEDITING/编码 artifact/插件 seam | fut | 第二 caller/测量+spec/第二部署方 | 触发条件先行 |

### 4.10 分层存储与无上限历史（21 项 · ~10%）〔区 D/K〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| CHAR-2/3 表征（无行） | acc | 近上限 canonical/修订 的 browse/details/paste/缩略图/search/R3 hydration 地图；缩略图整仓 reset/迟到完成/行增长曲线 | PLAY-STOR-2..4 receipts |
| TIER-1 四维预算（无行） | acc | logical/物理/cache/resident+in-flight 四维独立安全限与维度混填拒绝 | PLAY-MEM-1..7 |
| TIER-3 元数据-only 分页（无行） | acc | 一页 browse 0 内容字节 + scalar 行数≤limit+余量 | PLAY-STOR-2 + COUNT-7A（含 tie） |
| TIER-4 加权许可（无行） | acc | 源/工作区/输出分别计费、单请求超限 typed 拒绝、四释放路径、无 hold-and-wait、交互 paste 优先 | PLAY-MEM-* 逐叶 |
| TIER-5 内存压力响应（无行） | acc | warning 清冷未租 / critical 清可重建+暂停重预fetch；hosted DispatchSourceMemoryPressure 真实投递 | 注入+hosted 双 lane |
| TIER-6/PERF-7 ThumbnailStore 准入（无行） | acc | completed-only 500/64MiB 触发整仓 removeAll、迟到落逐出后、删除不精确修剪 | 05-DE4 裁决后实现；禁指纹盘身份 |
| ★ TIER-7 四个 O(N) 替换（无行） | acc | dedup 签名候选投影、捕获全量库存→事务聚合+受害者 cursor、search 全语料快照→索引候选+有界 top-K、策略扫全量解码→有界批 | PLAY-COUNT-1..5D 族（forced-collision 首捕仍合并） |
| TIER-8 UI 行窗口 + cursor 语义（无行） | acc | rows 持续 append 非可见窗口+lookahead；连续写下 cursor 无漏页/重复；pin 移动不重写全部 ordinal；验证不驻留全 ID 集 | COUNT-7A-7C/8A/8B（20k 行窗口不增长） |
| ★ DEC-U-SCALE-STARTUP-INDEX（blocked） | des | (a) P1 checkpoint 限 capped-profile + U-scale 换持久索引查询 或 (b) 修订 V2-06 P1——二选一未记录（前置于 PLAY-COUNT-1；禁双真相索引） | exclusive 决策记录 |
| TIER-9 开盖政策面（§22 L339） | acc | count=nil 仅测试门；用户最大 unpinned 与全局硬上限双限修订、真实 5,001st、共享 PLAY-DISK 门+尺度/soak | TIER-7/8 绿后 |
| Card 2F / DATA-8 判别实验（无行） | acc | Storage hard content ceiling：先修容量可见性+测代表 store overhead，达阈值才转 TDD 卡 | 决策实验（验证器 B 补） |
| PLAY-DISK-3 cleanup priority（无行） | acc | 空间不足先删可重建 DerivedCache、committed item 仍可读；不自动删 oldest | Red→Green（验证器 B 补） |
| DATA-12/05-CE25 R3/回填 RSS（无行） | acc | N×8/64MiB 超 R3 阈值 sweep 与 V1→V2 大 blob 回填峰值 RSS/墙钟/reopen | Release-child 矩阵 |
| STORAGE-3 lineage hydration 成本（无行） | acc | 共享 hydrate 统一拉 Canonical+全修订：64/128/384MiB 边界峰值+settled RSS | 超预算才加 purpose-specific 读 |
| STORAGE-4/05-FC18 跨岛聚合准入 | acc | 同 key 合并/单飞/无界观察 Task 不构成聚合界；overlap-aware 资源门 | 自有 resident+in-flight 准入 |
| 四平面字节上报 + 维护页 + 触发矩阵（无行） | acc | logical/物理 store-family/derived cache/resident 分开（含高水位）；维护页标注不可用；时间/内存压力永不触发清扫 | REPORT-4BYTES/MAINT-DISPLAY/TRIGGER-MATRIX |
| 05-ART9/ART11/TS9 资源报告+容量/备份 fixture+soak（无行） | acc | TieredStorageResourceReport、APFS 容量/ENOSPC/备份含排除恢复 fixture、24-72h soak 曲线（capture 段 ENOSPC 今日已绿勿重做） | 逐 artifact |
| PLAY-TIER-1A/1B pre-G8 表征（无行） | acc | decoder access-mode characterization（具体 decoder/fixture/OS trace）与 pure access-plan | 04 §26 允许先做（2A-THUMB 已 Done） |
| P3 支线 TIER-10..17 + G8 | fut | blob depot/lease/GC/迁移/备份全线被 G8 admission 记录挡 | 无触发则整支跳过 |
| 50k→250k→1m 阶梯 + 诚实文案 | fut | 逐级独立子进程 soak；"无固定条数上限·受容量与安全保留约束" | 前级全过才开 |
| LANG-FREEZE / NO-GOD-CACHE / 备份单元 | des | 四平面术语冻结、禁中央混合缓存、禁称 .externalStorage 自动冷层、备份单元/安全删除边界 | spec 冻结 |

### 4.11 State-3 发布与签名（9 项 · ~10%）〔区 H〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ★ Developer ID 发布链（§22） | acc | 冻结 bundle ID/版本/构建/图标/entitlements 契约、protected ref archive、全代码签名+Hardened Runtime+secure timestamp、notarize/staple/spctl、干净机复验+smoke | REL-1/P6-91 单链走完留证 |
| Signed state-3 证据 run（05-LAD3/EV7） | acc | 无签名 state-3 run 支撑 Gatekeeper/final entitlements/login item/TCC/升级；签名绝对 latency/RSS 缺 | state-3 run 归档 |
| App Sandbox 决策 + 迁移矩阵（P6-91-SANDBOX） | acc | 沙箱取舍未决；签名配置下 store 迁移/pasteboard/SMAppService 矩阵 | DEC-DISTRIBUTION 子决策 + 矩阵 |
| Entitlement 第三层门（05-CPD6/PD14） | acc | cloudKitDatabase 三层门只闭合前两层；签名产物 entitlement 检查未入 release gate | release gate 加验证 |
| VO/FKA + String Catalog 同旅程发布门（P6-93） | acc | 所有控件稳定标识/标签/焦点序/键等价；VO 与 FKA 完成 summon-find-copy 同旅程 | 旅程完成判据 |
| DEC-DISTRIBUTION 裁决 | des | Developer ID 直发 vs MAS 的 spec 裁决 | ADR 六字段 |
| 单一发行路径 scope | des | 先只 Developer ID 直发；禁同时 MAS/Homebrew/Sparkle | Phase-1 约束入 spec |
| Updater/MAS/Homebrew | fut | 未来 updater 的 feed/签名/artifact 出自同一发布事务 | 独立决策 |
| 导出功能 | fut | 需求/格式/授权三设计先行 | 获批后加入 |

## 5. 文档-账本冲突复核记录（验证器发现，领取前注意）

1. 07 §8.1/L837 称 audit barrier "未落地"，账本 X.6（L246）已 Done——**以账本为准**，仅
   crash-window 叶仍开。
2. GOV-1 的 Exact A/B 与 5k evidence job 是"被跳过的 dispatch job"，与 §20 两格无关，勿混。
3. 01 多条 acceptance 级发现无账本行（UI-2/UI-7/DATA-5a/10/7a/9a、CLIP-8/9、APL-C-05）——
   领取前必须先回 04 铸叶。
4. P0-0.3/P0-0.4（`DEC-RET-READ`/`DEC-PREVIEW-TARGET`）账本无处置行，而 unified-retention
   叶已在消费 readback seam——优先裁决。
5. 05-CE22 要求 WS13 附 zero-publish counter，账本 Done 行上限未列——docs 门更窄。
6. CLIP-2 要求"删除或收紧 public capture() convenience"，Card 5B Done 行未覆盖该收缩。
7. 09 TIER-12 要求每阶段 typed-fail；docs/05 §16 已接受 mid-transaction 耗尽为 crash
   ceiling——账本更窄且为已记录上限，blob-lane 各阶段仍须单独证。
8. V2 progress/roadmap 是否列全已落地集（X.4–X.8、F0A）无行可查——P0-0.2 V2 侧未证伪。
9. PERF-9/FC8/FC4 无行；账本 perf 行只声明"不证明性能"。
10. 05-FC6/GOV-4 snapshot 深锁、05-FC7 RSS 正值、05-FC10 bot 触发均无行。
11. DATA-0 的 manifest 级 entitlement 负门无行（只有 ad-hoc lane 负门 + 源码 `.none`）。
12. UI-2 的现状描述以 01 原文为准（评审时点）；领取时先复查当前实现再写 Red。

## 6. 今日（2026-08-23）已缩小清单（勿再计入缺口）

- Card 7/CLIP-1/X.7 的 "General Pasteboard 跨进程可见性" non-claim 移除（run 32632263996）。
- Card 6B 真实 APFS ENOSPC capture 叶转绿（run 32636093920）：真实竞争满盘下 §16
  admission 的 typed 拒绝、seed 不变、fresh-process reopen 全证。
- docs/05 §16 capacity admission（PR #25）：mid-transaction 耗尽从"未知"转为已记录接受
  的 Apple crash ceiling——调查负担转为诚实上限。
- APFS EXIT trap 定性 breadcrumbs-only（PR #27–#29）；instrumentation 行 Done。
- 账本 §7/§20、instrumentation、PROGRESS.md Batch 19 同步（PR #30）。
- 注意勿双计：Batch 21 五叶已有 branch source 未 merge——按 §1 规则尚非产品能力。

## 7. 建议领取顺序（更新；与 00 §6 / 04 §4 一致，不另立新序）

1. **Batch 21 五叶 merge + macOS CI 收口**（最短路径的 5 个验收格）。
2. **两个 P0 裁决**：`DEC-RET-READ`、`DEC-PREVIEW-TARGET`（GOV-2a/b；账本补处置行；
   顺带解锁 Card 10A 与 ContentPreview target）。
3. 区 0 文档项 + GOV-1 残留（绿 SHA dispatch Exact A/B 与 5k job 并记录）。
4. Card 9B external ingress purge + UI-16 VO 契约（release blocker 面）。
5. X.9 链：F1 credential custody → authentication → 只读 browse（衔接 DEC-PY-TRANSPORT）。
6. 首个 XCUI journey tracer + Card 16A archive 契约（进入 state 3 的门槛）。
7. 资源界 characterization（PLAY-STOR-2..4、PLAY-TIER-1A）与 Card 11C regexp 判别。
8. 区 J/K 在各自决策门裁决后开工。

## 8. Apple API 官方文档核实记录（两轮累计）

第一轮表（`supportedModes`/`IntentModes`/`AppDependencyManager`/`TransientAppEntity`/
`NSWindow.CollectionBehavior` 五旗标/`SMAppService`）继续有效，见 git 历史（本版未
删改其结论）。本轮新增：

| API | 核实结论 | 依据 |
|---|---|---|
| `URLResourceValues.volumeAvailableCapacityForImportantUsage` | 仅启动卷维护 purgeable 会计；**挂载/非启动卷实测返回 0**（run 32634051113），不可作 admission 事实 | Apple "Checking Volume Storage Capacity" 语义 + SO 56474397 同案 + 真实 run 证据 |
| `URLResourceValues.volumeAvailableCapacity` | 原始可用字节（与 df 同源）；全卷有效，admission 采用 | 同上 + run 32635233048 起实证 |
| Core Data external-storage interim file | 满盘创建失败抛 `NSInternalInconsistencyException`（非 NSError）→ 进程终止；throw site `+[_PFRoutines writePFExternalReferenceDataToInterimFile:]` | run 32632262141 保留 stderr（无官方文档——正是 05 memo UNKNOWN 预言的空白） |

## 9. 本审计的证据上限与复验记录

- 未在本机运行任何 Swift 构建/测试（Linux host）；"绿色"全部引用真实 GitHub run。
- **本版差距盘点方法**：13 个并行读手（00+README/01/03/04/05/06/07/08/09/账本 10）+
  合成 + 2 个对抗验证器（ledger 准确性 / 文档完备性）的 workflow；其中 04 与 06 的
  读手中途被 API 限流，其内容由验证器 B 直接以 04 原文补齐 11 条修正（Card 8C、
  11C/PERF-3、Card 14 族、2B-2、Card 17 gate 6/7/9、PLAY-PY E5R/E5W/E6A/E6B、
  2F/DATA-8、PLAY-DISK-3、PLAY-TIER-1A、3D 错挂修正），并由生成 agent 以账本 §21/§22
  与第一轮地图交叉核对；验证器 A 另补 2 条 Done-行 ceiling 尾巴（Card 6 banner
  物化、retention singleton 重启）。
- §2 的 grep 复核在 master `53c347d` 上执行（unmerged 零命中、`Task.sleep` 零命中、
  `ClipboardIntentModels` L70/81/86、`PasteboardMarkers.swift:20`、Settings
  `.success` ×4）。
- 完成度百分比为**粗估**（已关 leaf vs 本表聚合项口径），不是账本口径；引用以账本行为准。
- 04 卡片语义、03 DEC 表、05 盲区清单均以原文为准；本文件只做状态映射与新事实登记。
- 后续更新：请以下一个实现 PR 修正过时行；两格证据绿后，第一轮 §2.1/§2.4 中对应
  项已按本版 §2 复核关闭或保留。
