# AI 复审 TODO Map（2026-08-23 · 晚间全量差距版）

> **本文件由 AI 生成**：第一轮由 ZCode agent（模型 GLM-5.3，builtin:bigmodel-coding-plan）
> 于 2026-08-23 09:47–10:10 +0800 生成；**第二轮全量更新**由 Claude Code
> agent（模型 GLM-5.3）于 2026-08-23 18:30–20:45 +0800 基于 13 个并行读手 +
> 2 个对抗验证器的差距盘点 workflow 重写（方法与证据上限见 §9）。**第三轮
> （2026-08-29）为全量标记刷新**：§4.1–4.7 的每一行都对照活账本 §21/§22、
> PR #31–#54 正文与已合并代码逐行复核——已解决项划线并附证据指针、部分解决项
> 改写"差什么"列、policy-barred 项标注 AGENTS.md 2026-08-24 实现政策。它仍是一次性的
> **时点审计 + 待办地图**，不是活文档。
> [`10-implementation-status.md`](10-implementation-status.md) 仍是 REVIEW 中唯一活实现账本，
> owning spec（`docs/`、`docs/v2/`）仍高于本文件；两者冲突时以账本与 spec 为准，并请以
> 下一个实现 PR 修正本文件的过时行或将其归档。
>
> **快照基线（第三轮标记刷新）**：master `8b7486a`（PR #54 合并，2026-08-29）。
> 本轮覆盖 Batch 21/30–48（PR #23、#30–#54）的落地：面板外观/捕获 UX 批（PR #51）、
> open-store characterization/Row AX/DATA-0 负门/external-clone 验证（PR #52）、
> DEC-REVERT-RACE/DEC-UNPIN-SWEEP/regexp `.searchEngineDeadline`/StoreRoot permission/
> retention ingress 裁决（PR #53）、mid-transaction kill 双窗口/满盘 dispatch cells/
> GOV-3 surface 收缩/DEC-THUMB-CACHE 测量叶（PR #54）。
> 第二轮基线为 master `53c347d`（PR #30，2026-08-23）；第一轮基线为
> `cda2ba0` → `a3e6774`（PR #16）。

---

## 0. 本版更新摘要

- **第四轮（2026-09-03，Batch 49 / PR #58 单点标记）**：关闭 §4.2 F1 client
  credential custody、§4.5 DATA-7a 跨进程单写者租约、§4.7 GOV-3 ThumbnailStore
  尾巴；§4.1 Card 10 与 §4.3 Card 6B 两行按落地改写；§7 第 1/2 项划线。
  APFS dispatch lane 归档 PR #54 满盘 cells（run
  [33689980610](https://github.com/GuangDai/Clipy/actions/runs/33689980610) 绿），
  并顺带关闭 §16 admission 的 URL resource-values 缓存陈旧缺陷（首轮 dispatch
  run [33687222086](https://github.com/GuangDai/Clipy/actions/runs/33687222086)
  暴露：满盘前读数被缓存复用 → 满盘 revise 放行 → external-storage NSException
  SIGABRT；fresh-URL 逐读后 typed 拒绝）。批次证据：PR correctness run
  [33689990278](https://github.com/GuangDai/Clipy/actions/runs/33689990278) 双 job 绿。
- **第三轮（2026-08-29，基线 `8b7486a`/PR #54）**：§4.1–4.7 全量标记刷新（用户指示：
  暂停新内容实现，先把记录更新完）。已解决项划线收口并附 PR#/run#/账本行证据；
  部分解决项的"差什么"列按 Batch 41–48 的实际落地改写；仍开放/被挡项如实保留。
  §4.1 约 90% 关闭（16 项中 12 划线）；§4.4 的 shortcut/localization/VO 契约余量、
  §4.6 的 policy-barred 面、§4.7 的 ThumbnailStore 收缩与 DEC-THUMB-CACHE 裁决
  为剩余可领/待决项。03 §13 的 DEC-CAPTURE-CLOCK 过时 OPEN 行同步修正。
- 第二轮（2026-08-23）：§2 复核第一轮发现；§3 总览刷新至 PR #30；§4 重写为
  全量差距盘点（11 个 canonical 面积、~134 条聚合缺口，取代第一轮的 区 0/A–L
  框架，旧区字母以〔区 X〕标注）；新增 §5/§6/§7。

## 1. CI 真相基线（gh CLI 核实，2026-08-23 20:00 前后 +0800）

- **两个 dispatch-only 证据 cell 当日转绿**：
  - General pasteboard 跨进程可见性：run
    [32632263996](https://github.com/GuangDai/Clipy/actions/runs/32632263996)（首次绿）。
  - Card 6B APFS ENOSPC capture 事务叶：run
    [32636093920](https://github.com/GuangDai/Clipy/actions/runs/32636093920)（证据行
    `pressure_capture=transaction rejected with seed preserved`、
    `fresh_verify=seed state readable`）。
- master correctness 绿：PR #30 合并后 push run
  [32636602299](https://github.com/GuangDai/Clipy/actions/runs/32636602299) 三 job 全绿
  （完整重跑；其首次 attempt 与 e915848 的 push run 均因 `HistoryViewStateTests`
  pollUntil 簇超时而红——同 commit 重跑即绿，判为 runner 慢窗抖动，见 §2.3）。
- 当日 PR 链（全部 PR-run 三 job 绿后 merge）：#24（修复两处字符串插值语法错误 +
  NSError 反射断言）、#25（docs/05 §16 stamped-plan capacity admission）、#26（raw
  `volumeAvailableCapacity` 事实）、#27–#29（APFS EXIT trap 子 shell 触发三连修）、
  #30（账本/文档同步）。
- X.8/F0A/Batch 18–21 链（PR #17–#23）见账本 §16–§21，本版不重抄。

## 2. 本轮审计发现（两轮合并，按 master `53c347d` 复核）

### 2.1 已解决（第一轮发现，现核实关闭）

- **TODO-DOC-1（X.7 文字漂移）**：五处 "unmerged/未合并" 表述已随后续 PR 消失
  （本轮全库 grep "unmerged" 零命中）。
- **LEGACY-S-8（dwell 测试真实 sleep）**：`PreviewPaneStateTests` 已改用有限
  scheduler-turn oracle（Batch 19 tests-only 改动），`Task.sleep` 零命中。验收器对
  第一版盘点"仍列为缺口"的质疑成立，本版从缺口清单移除。

### 2.2 仍开放（第一轮发现，本轮 grep 复核仍在）

- **UI-10 文字面**：[`FloatingPanel.swift`](../../../ClipyApp/Sources/Panel/FloatingPanel.swift)
  注释 "every space" vs `.moveToActiveSpace` 的措辞与三处文档不一致（Apple 文档判据
  见 §8）；真机 Space/Stage Manager/全屏矩阵未跑（归 §4 面积 3/4）。
- **TODO-X7-1（X.7 实体保真）**：`ClipboardIntentModels.swift` 仍硬编码
  `title = "Clipboard Item"`（L86）与 `revisionCount = 0`（L70/L81）——BLOCKED-SPEC
  （须先扩 HistoryCore 读 DTO 并过 symbol snapshot）。
- **APL-C-05**：`PasteboardMarkers.swift:20` 仍称 "the three NSPasteboard framework
  markers"；应改为第三方尽力 denylist 措辞 + 真实密码管理器矩阵（归 §4 面积 5）。
- **GOV-3 残余**：Card 5D 只收窄了两个 pasteboard knob；完整 Release/signed symbol
  与其余 test instrumentation 审计仍 Open（归 §4 面积 7）。
- **Card 10 Settings**：`ClipySettingsView` 仍是无 per-field dirty bits 的
  `status = .success("Done.")` 模式（×4 处）；10A–10E 六 leaf 全 Open（10A 前置
  GOV-2 的 `DEC-RET-READ` 裁决）。

### 2.3 当日新事实登记（细节以账本/AUDIT/PR 为准，此处只登记）

- **Core Data external-storage 满盘异常**：run 32632262141 证明满盘 + 大 blob 时
  Core Data 抛未捕获 `NSInternalInconsistencyException`（"Can't create
  externalDataReference interim file : 28"）→ SIGABRT，而非 NSError——`Persistence
  ErrorClassification` 根本没机会跑。产物：docs/05 §16 stamped-plan capacity
  admission（PR #25）+ `CaptureCapacityAdmissionTests`。**admission 通过后的中途
  耗尽是已记录接受的 Apple framework crash ceiling**（AUDIT §3），不是可领工作。
- **容量事实键反例**：`volumeAvailableCapacityForImportantUsage` 在挂载卷返回 0
  （run 32634051113；purgeable 会计只在启动卷维护，SO 56474397 同案）→ admission
  改用 raw `volumeAvailableCapacity`（PR #26）。
- **bash 3.2 子 shell EXIT trap**：macOS runner `/bin/bash` 3.2 在替换子 shell
  中途触发继承的 EXIT trap（runs 32634454727/32635233048/32635568571 三次实证；
  `$BASH_SUBSHELL` 不递增、无 BASHPID）→ APFS 脚本 EXIT trap 定性为
  breadcrumbs-only（PR #29）；**勿在任何 CI 脚本的 trap 中 reintroduce 资源 teardown**。
- **`HistoryViewStateTests` rerun-flake**：两次 push run（32636092247、PR #30 CI）
  在 ~11:20–11:26 UTC 窗口成簇 pollUntil 超时，同 commit `gh run rerun --failed`
  即绿。处置约定：先重跑再诊断。
- **Swift 单行字符串插值不可含换行**（PR #24 两处）；ObjC 类 metatype 反射不带
  模块前缀（`swift_type="NSError"`，非 `Foundation.NSError`）。

## 3. 状态总览（review 判定 → 当前，PR #54 后，2026-08-29 刷新）

- **P0-0（红色 SHA）**：已修复（PR #2–#16）。
- **正常路径 correctness**：§4.1 的 16 项聚合中 12 项已划线关闭（PR #23/#32–#35/
  #40–#44/#46–#48/#50–#51/#53），含全部四项语义裁决（DEC-OBSERVER-START/
  CAPTURE-CLOCK/PASTE-REFERENCE/REVERT-RACE/UNPIN-SWEEP）、Card 11C 的
  `.searchEngineDeadline` 产品修复、running-app Details/retention/keyboard
  journeys。余 3 项 partial（unified retention 的 localization 尾巴、Card 3D
  hosted copy 可见性、Card 10 的 launch 四态矩阵）。
- **签名隐私/capture（CLIP-1/5A/6B）**：Card 6B 满盘 capture 叶绿；revise 满盘
  admission 拒绝 + open 满盘 characterization 的 probe/脚本已落（PR #54，断言
  待 dispatch run 归档）；5A signed 四态、TCC、发行身份恢复仍开（ci-impossible）。
- **物理/跨进程证据**：pasteboard 跨进程、APFS capture、external-clone 全量
  验证（PR #52）、mid-transaction kill 双窗口（PR #54）、store-open 三维
  characterization（future-schema/corrupt/permission，PR #52/#53）绿；签名/
  真机矩阵仍开。
- **资源界（Card 11/12/13、STOR/MEM）**：11A admission+poison probe 关；
  DEC-THUMB-CACHE 测量叶已落（PR #54，60-PNG 滚动 journey 导出重复率与
  p50/p95），G1 裁决本身仍 OPEN；dedup/search 的 O(N) 替换族仍开（§4.10）。
- **Gateway/Python（X 系）**：X.2–X.8 上限内 Done；F1 server 侧 custody/
  enrollment 拒绝、authentication kernel、内部 authenticated browse、audit
  crash-window 均关（PR #35/#37/#38/#41/#48）；client 侧 custody 文件、对外
  ingress facade（DEC-PY-AUTHENTICATED-INGRESS 仍 BLOCKED-SPEC）、opaque
  locator、clipyctl/transport 仍开（§4.2/§4.8）。
- **Formats/Preview**：`ClipboardFormats` stable facts + ContentPreview 深模块
  （PR #33）+ 聚合输入 admission（PR #50）；其余 ~85% 未动（§4.9）。
- **Tier/U-scale/P3**：characterization 可领；TIER-2B+ 全部 BLOCKED-SPEC/G8；
  DEC 注册表大量行已裁决（含 2026-08-28 双 DEC），余量见 03 §13。
- **State 3**：除 `SIGNED-RUNTIME-0` 有界 lane 外全部 Open；产品不可分发
  （发行机制按 AGENTS.md 2026-08-24 政策须用户显式要求才启动）。

## 4. 全量差距盘点（11 面积 · ~134 聚合项）

> 层级：**acc** = 当前-lane 验收叶（可领）；**fut** = future-work（被评审自己的
> approval/evidence 门挡住，不可当当前 lane 领取）；**des** = design-only（须 spec/
> ADR 决策先行）。状态来源：账本行号（L…）或"无行"（评审明文要求但账本无行，
> 领取前须回 04 铸叶或由实现 PR 补行，§23 规则 5）。★ = blocking 后续 lane 的
> 验收格。完成度为粗估（按已关 leaf vs 本表聚合项口径）。


### 4.1 正常路径 correctness 遗留（16 项 · ~90%）〔区 A/B〕

> 2026-08-29 修订（基线推进到 Batch 48 / PR #54）：本节 16 个开放聚合项中 12 项已
> 由 PR #23/#32–#35/#40–#44/#46–#48/#50–#51/#53 关闭并划线；余 3 项部分解决。
> 证据以账本 §21/§22 行为准。

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ~~★ Batch 21 五叶~~ | — | **已解决**（证据：PR #23 已合并（2026-08-23），correctness run 32623287645 全绿；账本 §21 五行全 Done：8B hosted 全置顶分页、9C hosted 面板 frame、11A search admission、unified retention draft、15D 捕获失败播报。余留 ceiling 由后续行/§4.4 承接：Card 8B 整体仍 Partial（非任意 scroll/AX/FKA）、15D VO 朗读/AX tree/FKA/localization 归 §4.4 "Card 15D 尾巴"行） | — |
| ~~Card 3B（L53）~~ | — | **已解决**（证据：PR #47（Batch 42，merge `1abd37a`）账本 §21 Batch 42 行 "editor stale/reload/dirty-cancel"：`EditorRuntimeJourneyUITests` 自述 "Cards 3B/3C/3D public-control evidence"——真实竞争修订使 Save OCC-stale、alert/footer 保留 literal draft 字节、typed Reload 失败保持可重试并恢复；master 32734578011 首跑 hosted flake 后自 32743395877 持续绿） | — |
| ~~Card 3C（L54）~~ | — | **已解决**（证据：同一 PR #47 journey：`testDirtyDismissalConfirmsAndCleanCancelClosesDirectly` 证明 Esc 与 Cancel 进入同一 dirty-dismiss 确认、无改动取消直接关闭；账本 §21 Batch 42 行） | — |
| Card 3D（账本 §16） | acc | ~~披露文案决策 + source literal~~ 已闭合（Batch 38 PR #40 merge `c89f2ba`：owning roadmap/AUDIT 批准 pre-Save 提示；`ReviseEditorView` 渲染 one Presentation-owned literal，`ReviseEditorDraftTests` 以独立 literal 固定"不删除 previous/original"边界）；**仍差 hosted copy test**——runner 上 real attached SwiftUI sheet 只暴露空 public AX `Dialog`，hosted copy 可见性不可证（账本 §21 Batch 38 行 Partial） | 换非 AX 依赖的可见性证据面（或签名真机/VO 取证） |
| ~~Card 9D/15 键盘恢复（L338）~~ | — | **已解决**（证据：Card 9D typed Retry 语义 Batch 40（PRs #42–#44，final master 32715020428）；真实 control+键盘 Batch 42（PR #47）：`PreviewRecoveryJourneyUITests` 自述 "Card 9D/Card 15 running-app"——可点击 Retry、app shortcut 键盘恢复、无 Retry 资格表面不伪造 Retry；账本 §21 Batch 40 Card 9D 行 + Batch 42 行。VO/FKA 仍归 §4.4） | — |
| ~~Card 8C（无行）~~ | — | **已解决**（证据：账本 §21 "Card 8C count + authoritative selection reconciliation" Done（Batch 40，PRs #42–#44，final master 32715020428）：captions 显示 `N+ items/results`、loading gap 不冒充 authoritative removal、selected 被真实 replacement 移除时 selection 置 nil 不跳 newest、Return 仍要求 exact current reference；上限：localization/真实 large-page scrolling/FKA 不证） | — |
| ~~Card 8G（L63/L87）~~ | — | **已解决**（证据：AX-exposed Clear + focus——账本 §21 "§4.4 Search Clear/focus + row AXPress leaf" 的 Clear/focus 格 Done（PR #41 merge `87db3d6`）：real XCUI 无 click 输入、Clear 恢复 recent rows 且重获 search focus（顺带修复 FloatingPanel keyboard-only `keyCode` 吞 click 的真实 bug）；autocorrection-off——`PanelLifecycleHostedTests` 经 real field editor 断言 `isAutomaticSpellingCorrectionEnabled == false` 且重开面板重申（Batch 42/43，PR #47/#48）。注：AXPress 格在无 Accessibility authorization 的 runner 上显式 skip，该 Partial 归 §4.4 行） | — |
| ~~Card 9C/9F 尾巴（L66/L315）~~ | — | **已解决**（证据：negative-origin 721pt expand/collapse geometry——账本 §21 Batch 40 行（PRs #42–#44）+ Batch 43（PR #48）`PanelAndHotKeyTests` 负原点屏 clamp；位置语义——PR #51（merge `f123410`）落地 normalized 0…1 `.lastPosition` anchor（仅 USER drag 位置持久化）+ 全模式 uniform visible-frame clamp + 大屏持久尺寸 reopen shrink-to-fit + last-position round trip 测试：不可达窗口面收口。落地形态为"屏内 normalized anchor + clamp"，非字面 "stable main-content rect" 坐标） | — |
| ~~Card 11A 尾巴（L316）~~ | — | **已解决**（证据：poison-probe——账本 §21 Batch 43 行（PR #48 merge `b0641e6`）"search-admission poison probe 叶闭合"，`SearchAdmissionBeforeIOTests` stored-corpus poison 区分 admission 与 corpus access；PERF-1 empty fast-path——Batch 42（PR #47）显式测试 empty term 直接 scalar read（同文件 PERF-1 注释行）；协作取消——Card 11B Batch 40（PRs #42–#44）每 32 行 checkpoint/publication fence + Batch 41（PR #46）cancelled search replacement read/capture 前向推进。上限照账本：native fetch/sort/单行 matcher 不可抢占；regexp 危险族已由 Batch 47（PR #53）typed `.searchEngineDeadline` 另行界定） | — |
| unified retention 尾巴（L317） | acc | hosted Settings journey 主体已大幅缩小：one Retention tab/shared draft + 单 field dirty 语义（Batch 39 PR #41）、running-app age-tightening strict 确认 + Esc 取消（Batch 38 PR #40）、count/R1/R2 running-app retention journeys（Batch 41 PR #46）、bounded retention 证据（Batch 44 PR #49）；~~receipt/purge external ingress~~ 已裁决不存在（47-4：路线 B，PR #53——Settings 经 v1 seam 是唯一 owner；防回归：`ExternalRetentionIsolationTests` + `ExternalAccessPolicyTests`/封闭 raw 词汇 20…999 天花板）。**仍差**：localization、AX/FKA、retained-byte usage、所有 strictness 组合（账本 §21 Batch 39 行上限列） | 后续 hosted journey + localization（与 §4.4 Localization 行同源） |
| Card 10A–10E（账本 §21 各行） | acc | ~~10A 前置 `DEC-RET-READ`~~ 已裁决（PR #32 Batch 30：existing `retentionConfiguration()` 为 purpose-specific public read；Card 10A consumer Done）；per-field dirty draft 语义与保存确认已落（`RetentionSettingsDraft` 单 field dirty + tightening confirmation（PR #23）；one Retention tab（PR #41）；strict warning `Apply Stricter Limits` + Esc（PR #40））；10C/10D running-app Settings controls Done（Batch 38 PR #40）。**Batch 49（PR #58）又收**：`.success("Done.")` 兜底清零——三处 receipt→feedback 映射改 exact per-receipt（`.unchanged` 出诚实 no-op 文案 "Nothing to clear."/"No change."，mismatch 归 failure rendering 且不推进 draft baseline）；loosen/count/storage/revision/equal 控制获 draft 单测（equal/no-op、per-field dirty 独立、逐维 loosen 无需确认）+ hosted `ComposedRetentionLooseningTests`（真 in-memory `SwiftDataHistory` 上 tighten→loosen→equal 全链，exact `retentionConfiguration()` readback + no-op 不推进 ChangePosition 证明）。**仍差**：launch-at-login 只证 materialization（off/on/requiresApproval/unavailable 四态、external refresh、signed install 未证）；localization/AX/FKA；retained-byte usage | 铸余下 leaf（launch 四态矩阵——signed；localization 随 §4.4 行） |
| ~~★ Card 9B external ingress（L105/§22）~~ | — | **已解决**（证据：账本 §22 第一行——Batch 33 已把当前唯一外部写入口 App Intents/Gateway positive remove 接入同一 owner（PR #35，PR/master correctness 32688665740/32688965362）；`AppIntentSurfaceInvalidationTests.removePurgesExactSurfaceBeforeHeldObservationCatchesUp` 证明 held observation 下外部 remove 在 intent 返回前 purge 真实 panel surface；off-page purge facts 由 purgeGeneration/late-flight fence 族守望（`HistoryDetailsPurgeTests`/`SettingsClearSurfacePurgeTests`，Batch 42 扩充）。§22 上限照旧：Observation 仍是 replacement state 非 public change feed；future Local Automation、restart replay、metadata-only refresh 不由该 source 外推） | — |
| ~~Card 11C/PERF-3/PERF-2~~（无行） | — | **已解决**（47-2 裁决：grammar 冻结不变 + 扫描换 `enumerateMatches` 可中断迭代器 + 2,000ms typed `.searchEngineDeadline`；PERF-2 的取消面由 11B 的 32-row checkpoint 与 47-2 的引擎内取消共同闭合；判别证据 run 32757784066/33177236846） | — |
| ~~Card 2B-2（无行）~~ | — | **已解决**（证据：账本 §21 "Card 2B-1/2B-2 candidate item-ID collision" Done（Batch 40，PRs #42–#44，final master 32715020428）：insert lane 以 complete retained inventory 拒绝 occupied candidate、Authority 转 internal remint signal、facade 最多 8 次 total attempts 复用 prepared bytes/fingerprints、耗尽 zero write/publish；pure coalesce/insert + real in-memory facade tests 钉死；不把 fingerprint 当 identity） | — |
| ~~UI-2（无行）~~ | — | **已解决**（证据：Batch 40（PRs #42–#44）账本 §21 "UI-2 Details mutation pending owner" 行——one pending state/ProgressView/disabled+reentry guard，receipt→details readback 顺序保留（`HistoryViewState` "read/transition after the real receipt (review UI-2/Card 9B)"、`HistoryDetailsView` "Sequences the destructive mutation before its readback (review UI-2)"），Remove unchanged/failure 强化 zero purge；Batch 41（PR #46）`DetailsMutationJourneyUITests` 自述 "UI-2's running-app vertical proof"——public mouse controls 经 Unpinned→Pinned→Unpinned→removed、receipt-ordered details readback、Remove confirmation、exact surface purge 全为 production path） | — |
| ~~DATA-5a / DATA-10 / CLIP-8 / CLIP-9（无行）~~ | — | **已解决（四项裁决齐）**：DATA-10 `DEC-OBSERVER-START` RESOLVED（03 §13 注册表：startup/Retry 立即导入 current complete generation，Resume 只 baseline changeCount；PR #50 Batch 45 "observer-start 语义冻结证明"）；DATA-5a `DEC-CAPTURE-CLOCK` resolved（V2-02 DC-28：capture age 以 admitted `observedAt` 为唯一时间事实，rejected Authority-admitted-time 与 finite-skew clamp；Batch 45 PR #50 `RetentionCaptureCompositionTests` 冻结证明；**注：03 §13 注册表该行仍标 OPEN，属未同步的过时行**）；CLIP-8 `DEC-REVERT-RACE`/`DEC-UNPIN-SWEEP` RESOLVED 2026-08-28（V2-02 §4.3/§7 + 两个穿真实 Authority seam 的判别测试；PR #53）；CLIP-9 `DEC-PASTE-REFERENCE` RESOLVED（current-by-ID at the Authority read，displayed reference 只做 submission admission；03b 记录 + Batch 41 PR #46 落地） | — |
| ~~LEGACY-S-8~~ | — | **已解决**（Batch 19 有限 scheduler-turn oracle；§2.1） | — |

说明（证据口径）：

1. 划线行的权威依据依次为：账本 §21 对应行（Batch 21 五叶 = PR #23/run 32623287645；Card 9B = §22 第一行 + PR #35；Card 8C/Card 2B-2/Card 11B/UI-2 = Batch 40 PRs #42–#44 final master 32715020428；Card 3B/3C/9D-15/PERF-1/autocorrection = Batch 42 PR #47；poison probe = Batch 43 PR #48；四项 DEC = PR #50/#53/#46 + V2-02/03b/03 §13 记录）；代码核实项：`AppIntentSurfaceInvalidationTests`、`EditorRuntimeJourneyUITests`、`PreviewRecoveryJourneyUITests`、`DetailsMutationJourneyUITests`、`SearchAdmissionBeforeIOTests`（PERF-1 注释）、`PanelLifecycleHostedTests`（field editor autocorrection）、`PopupPositionGeometry`/`FloatingPanel`（normalized anchor + clamp）。
2. 三行保留为部分解决：Card 3D（差 hosted copy 可见性，runner AX `Dialog` 空是已记录上限）、unified retention 尾巴（差 localization/AX/FKA/retained-byte usage/全 strictness 组合）、Card 10A–10E（Batch 49 后只差 launch-at-login 四态矩阵——signed install——与 localization/AX/FKA/retained-byte usage；loosen/count/storage/revision/equal 控制与 `.success("Done.")` 清零已由 PR #58 关闭）。
3. 无 UNVERIFIED 行；唯一发现的账本外不一致是 03 §13 注册表的 `DEC-CAPTURE-CLOCK` 行仍标 OPEN（V2-02 DC-28 已 resolved、Batch 45 行已证），已在对应行内标注"待同步"，未据此虚报。
4. 节标题完成度由 ~65% 改为 ~90%（粗估口径不变：16 聚合项中 12 项关闭、3 项部分解决）。


### 4.2 External Gateway X.2–X.8 后续（7 项 · ~50%）〔区 I〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ~~★ F1 client credential custody（L294）~~ | — | **已解决**（Batch 49，PR #58：package-only `LocalAutomationClientCredentialCustody`——dir 0700/file 0600（tighten-on-save + reject-on-load）、no-follow 拒 symlink/非正规文件、file-owner==dir-owner 校验、原子替换（0600 临时文件 + `replaceItemAt`/move + 逐字 readback、readback 失败即删并拒发布）、继承 stdin 供应（纯 `Data` 验证 + 薄 `FileHandle` 层）、authority-first 撤销（`removeCredential()` 永不抛、只报路径缺席）、crash-orphan 回收仅限精确临时命名族；16 个 exact file 测试 + orphan/decoy fixtures；correctness run [33689990278](https://github.com/GuangDai/Clipy/actions/runs/33689990278) 绿；spec 依据 V2-05 §0.3 + 07 §5.1）。上限：无 CLI/ingress/transport/placement（07 §6.7 仍 BLOCKED-SPEC） | — |
| ★ Authentication + AuthenticatedIngressFacade（L294） | acc | credential 比对已闭合（Batch 36 in-process kernel：PR #38 merge `1834eca`，final/master correctness 32694144024/32694673199；`LocalAutomationCredentialAuthenticator` 解析 exact UUID16+secret32，active/revoked→ID、malformed/missing/wrong/orphan→nil，auth 前后 Gateway snapshot 相等）；内部受限 join 已闭合（Batch 39/PR #41 `LocalAutomationAuthenticatedBrowse`，源码自述 "deliberately not an ingress facade"）。仍缺：对外受限 ingress facade/neutral package-only DTO/placement——`DEC-PY-AUTHENTICATED-INGRESS` 仍 BLOCKED-SPEC（custody/publication 方向已冻结、实现与 signed evidence 开放，继续阻塞 production transport/CLI 正向 tracer，07 §10） | DEC 解冻后铸薄 facade 委托唯一 ExternalGateway；kind-mismatch/预算拒绝面已前置钉死（Batch 17 `GatewayConnectionKindRecheck/RateDenial`、Batch 43 `LocalAutomationCredentialKindIsolationTests`——exact App Intents bytes 不能跨 kind 认证，拒绝发生在 Gateway audit 前） |
| Opaque locator/cursor（07 L435-441/L832） | acc | Gateway 铸造 locator/cursor 未实现；X.8 wire 上限 4,096B vs `HistoryPageCursor` ~26KiB + locator lifetime 未冻结（挡正向 B4/B5） | spec 裁决（bounded server-side state 或 versioned encoding）+ C2–C5 Reds（禁 hash 捷径） |
| ~~X.9 只读 positive browse~~（§22 L347-350） | — | **已解决**（Batch 39 内部纵向叶：PR #41 merge `87db3d6`，final PR/master correctness 32705015919/32705436579；账本 §21 “§4.2 authenticated Local Automation browsePreview leaf” 行——Authority 仅在 custody-verified seam 以 preassigned ID 原子发布 `.localAutomation` 行+成功 audit（=enroll），`LocalAutomationAuthenticatedBrowse` 把 exact credential 绑到唯一 `ExternalGateway` recent/search `browsePreview`，real V4 tests 钉 granted positive page+audit（=grant/browse）、wrong unaudited、no-grant/revoked audited denial（=deny/revoke）；Batch 43/PR #48 再补 admission/publication-barrier/kind-isolation 有界测试。上限：内部叶，不发布 ingress/DTO/framing/transport/CLI，不等于 X.9 外部交付——账本 §22 X.9+ 段（clipyctl/process I/O/transport/runtime）仍开放，归本节 ingress/locator/signed 各行） | — |
| Signed DPK Keychain 往返（L294 ceiling） | acc | signed artifact 的 DPK persistence/reopen/delete、access-group/profile、client 可读性（现测试不穿真 Keychain——Batch 18 账本行上限至今未变） | Developer ID/profile 矩阵取证 |
| X.7 signed-runtime cells（L263/L340-346） | acc | framework system-manager resolution、真 Siri/Shortcuts 冷/热 invocation、discovery、process placement、Swift 6 queue-crash、TCC（账本 §22 现行版本原文重申这些格未关；Batch 41–48 无 signed X.7 矩阵行） | signed 矩阵逐格 |
| ~~Audit crash-window~~（07 §8.1/§14） | — | **已解决**（Batch 43：PR #48 merge `b0641e6`，master correctness 32743395877 绿；`GatewayAuditAndLargeBlobCrashRestartTests.readAuditCommitPrecedesPublicResultAcrossCrash` + probe `gatewayAuditCrash` 以 DEBUG TaskLocal `afterSynchronousSuccessfulAuditCommit` 在 V2-05 §5.2/PLAY-PY-D5 精确边界——read audit 已 durable commit、`ExternalReadResult` 未过 Authority method boundary——杀子进程（`terminationReason == .uncaughtSignal`），fresh owner 经公共 admin facade 恰见一条 succeeded read audit；append-failure 不释放 DTO 的另一半由 X.4 注入叶 `auditAppendFailureIsThePublicationBarrier`（`49c5cec`，本表基线前已绿）钉死。上限：只证 pinned macOS/SwiftData lane 的 process-crash，不断言 fsync/断电/sidecar 布局或 client 完整接收） | — |


### 4.3 物理·运行时·发行证据（14 项 · ~20%）〔区 C/G/H〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| Card 6B open/migration 满盘（L138/§22） | acc | store open 满盘 cells **已归档**（PR #54 probe `openFullVolume`/`openSeededFullVolume`；Batch 49 dispatch run [33689980610](https://github.com/GuangDai/Clipy/actions/runs/33689980610) 绿：两格均记录 `*_OPENSTORE_REFUSED` 观测分支——满盘 open 的 typed 拒绝经 `.persistence(.openStore)` 扁平，含 DATA-7a `.lease` artifact 创建 ENOSPC 同归）；V1→V2 migration 满盘仍无（无公开 API 路径可铸造 V1 store） | ~~disposable volume 格 + fresh-child reopen（dispatch run 待归档~~已归档；migration 维度无可铸造路径） |
| Card 6B revise/remove/clear 满盘（§22） | acc | revise 满盘 cells **已归档**（Batch 49 dispatch run [33689980610](https://github.com/GuangDai/Clipy/actions/runs/33689980610) 绿：满盘 8MiB `.replace` revision 经 §16 admission typed 拒绝 `.temporarilyUnavailable(.insufficientDiskSpace)`、seed 字节不变、空间释放后同一 revision 提交成功、fresh owner 复证——`pressure_revise_commit`/`pressure_revise_verify` 行）。**首轮 dispatch 暴露并已修一个真实产品缺陷**：§16 admission 的容量事实读自缓存的 URL resource values（共享 NSURL 首读即缓存）——revise child 满盘前读过 254MiB，满盘后 revise 凭陈旧值放行并触 external-storage `NSInternalInconsistencyException` SIGABRT（run [33687222086](https://github.com/GuangDai/Clipy/actions/runs/33687222086)）；PR #58 改为 fresh-URL 逐读 + probe 两格 GO 时刻容量观测行（capture 6,430,720 / revise 6,463,488 字节）。remove/clear 满盘尾巴明示维持不刻画（零需求 plan 永不 admission-refuse，PR #54 boundary）；mid-transaction 耗尽仍是接受的 crash ceiling | ~~revise 待 dispatch 归档~~已归档；remove/clear 不刻画 |
| Card 6B 发行身份环境恢复（§22） | acc | Developer ID 环境满盘恢复 | 恢复 cell |
| ★ Card 5A signed 四态矩阵（L298/§22） | acc | 真实 prompt、TCC/System Settings 切换、clean profile、跨 restart/login 的 AccessBehavior 四态（当日跨进程格明确不含 TCC；Batch 40/42 的 denied/pause 仅替换 test privacy posture，不触及） | signed clean-profile 矩阵 |
| Carbon/快捷键/Secure Input 矩阵（无行） | acc | 可配置 summon-shortcut 已完整落地（#47 controller/安全 swap+冲突警告测试 → #48 所有权接线+Settings 可见恢复 → #50 经安全注册事务的录制；失败保留旧绑定有 owner 证据）；真 Carbon 回调线程/布局/Secure Input 的 signed 真机 runtime 矩阵仍无证据 | macOS 26 真机矩阵（05-EV2/OQ5） |
| ~~★ 真实 XCUI journey tracer~~（无行） | — | **已解决**（证据：PR #36 merge `2bc4a8e` 建 XcodeGen `ClipyUITests` target，真实 runner summon→键入→Down→Return→General pasteboard exact→panel close；master 32692472789 绿；账本 §21 "Card 14A/14D + first running-app XCUI tracer" 行；#41/#46/#47/#53 持续追加 Settings⌘,/AXPress/banner/permission-recovery 旅程） | — |
| StoreRoot + 分类恢复 UX（§22） | acc | Retry/Reveal 同 locator 非破坏恢复已落地（PR #49 running-app 证明；PR #53 只读 StoreRoot permission pane + 恢复权限后可见 Retry journey）；corruption/permission/future-schema 三维已由 fresh 子进程穿公共 `SwiftDataHistory.open` + hosted characterization 钉死 `.openStore` 扁平（PR #52/#53；macOS 26 公开错误面仅 `loadIssueModelContainer`，`unknownDataStoreSchema` 要 macOS 27，DATA-14 维持扁平、无分类器/专属文案）；仍缺 ENOSPC/transient-I/O/WAL/sidecar 维度（ENOSPC 归满盘 lane） | DATA-14/05-OQ13-15（ENOSPC 维度随满盘 dispatch lane） |
| 子进程 kill/reopen 链（无行） | acc | external-blob crash-restart 已证（PR #48 largeBlob 8MiB crash→fresh child 字节精确 verify，master 32743395877）；terminated-seed 全量 rehydration 已证（PR #52 validateSeed/validateAll）；mid-transaction kill 双窗口已就位（PR #54：窗口 A 事务闭包内确定性 complete-OLD、窗口 B `willSave` 锚定 old-or-new 二值 + fresh child verify + DATA-13 可写尾步，待 correctness CI 绿；只证 process-kill，非断电/fsync）；仍缺 migration seed→migrate→kill→reopen（V1 store 无公开铸造路径）与 WS14 全形状 kill/reopen 矩阵 | DATA-13/05-CE23/26/REL-4 分段 tracer（migration 维度无可铸造路径） |
| ~~External-clone 验证子进程~~（无行） | — | **已解决**（证据：PR #52 merge `49577bd`、master 33182041623——probe `validateSeed`/`validateAll`：terminated seed 留 6 项混合 text/PNG/mixed/8MiB fixture + 8MiB replace revision，fresh child 经公共 `browse`/`details`/`pastePayload` 全量字节精确比对；按裁决用全字节比对而非 digest，且不断言 externalStorage 外置/sidecar 存在） | — |
| Card 10C 运行时格（L300） | acc | fresh install、external revoke、logout/login、真 System Settings、signed installed runtime（PR #40 已证 ⌘, Settings 控件物化，但明示不含这些格） | 签名安装态矩阵 |
| Card 5D instrumentation audit + Developer ID（L327-329） | acc | 26-literal 负匹配 lane（finite Release symbol workflow）连同符号生成/比对机制已按 2026-08-24 policy 移除（PR #41；AGENTS.md 禁仓库自建 signing/notarization 机制）；完整 instrumentation audit 与 Developer ID/distribution/runtime 证据仍缺 | Developer ID 全量 audit（外部产物证据） |
| ~~Card 6 banner 物化尾巴~~（L95 ceiling） | — | **已解决**（证据：PR #47 merge `1abd37a`、同代码自 master run 32743395877 起持续绿——`CaptureHealthBannerJourneyUITests` 真实 running-app：banner 物化、dismiss 控件 `clipy.capture.notice.dismiss` 可点、同 episode 不复现、恢复路径；账本 §21 Batch 42 行；强于原"hosted UI 物化证据"要求） | — |
| Retention config singleton 重启尾巴（L89 ceiling） | acc | true restart 已证（PR #41 `TrueRestartChildTests`：同一 StoreRoot 上四个 normally-terminated owners write→read→update→read，exact count+全 policy DTO+ChangePosition 0→2→4；master 32705436579）；migration 矩阵仍缺（disk-full/kill/crash durability 明确不在该叶内） | migration 矩阵格 |
| Panel Space/StageManager/多屏/FUS 矩阵（无行） | acc | workspace lifecycle owner（focus/power/login-session/screen 变化）已有 hosted notification 证据（PR #47），但明示非物理 sleep/FUS/WindowServer；`.moveToActiveSpace` 等行为旗标仍无真机矩阵，面板最小行为未选定（PR #51 面板批亦明示不证多屏/Spaces/Stage Manager） | 真机矩阵 + 行为选定（UI-10/05-EV4/OQ7） |
| 同机 A/B 三旅程（00 §6.10） | fut | 优越性证据 NONE；评审明排 LAST（gate 未解除：Card 16A 仍 In progress，无 protected release tag/实际 archive run，签名产物未就绪） | 签名产物就绪后三旅程 |

### 4.4 Panel·UI·AX·localization（9 项 · ~40%）〔区 A/F/G〕

> **2026-08-29 修订**（快照基线 PR #30 → Batch 48 时点；按账本 §21/§22（已记至 Batch 48，PR #54 OPEN、source/tests 已就位）+ PR #36–#53 bodies + 代码复核。Batch 48（PR #54）四叶均为 kill 链/满盘 cell/GOV-3 收缩/Thumbnail 测量，不触及本节各行）。复核后：5 行已解决、4 行仍开放（其中 3 行更新"差什么"）。

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ~~Card 15D 尾巴（L318）~~ → 保持开放 | acc | 原文不变：绿后仍只证 content-free notification seam——VoiceOver 实际朗读、AX tree、FKA、localization。（2026-08-29 复核：尾部四项仍零证据，PR #52 行明示"不证明运行时 AX 树/真 VO 朗读"；seam 家族已扩至 remove 播报（PR #35）、settled search-count 播报（PR #48），均仍只证 seam 不证朗读） | 真 VO 读出证据 |
| Row AX actions（L93） | acc | ~~per-ID AX identity/default Copy/具名 action 只有 source wiring~~ 部分解决：per-ID stable element + `.button` role + title label 已有 running-app 证明（账本 §21 Card 15A 行，PR #39，master 32696647481）；默认 action AXPress 有 running-app cell 但 runner 无 Accessibility authorization 时显式 skip（账本 §21 §4.4 行，PR #41，整体 Partial）；四个 `accessibilityAction` 已收敛 package 分发表 `performAccessibilityAction` 并直测 default→paste hand-off、named Pin/Remove/Details 与 ⏎/⌘P/⌫/⌘I 同 intent + admission 守卫（账本 §21 Batch 46 ②，PR #52，master 33182041623）。差：运行时 AX 树上执行具名 action（CI hosted 树为空 `AXGroup`，该无效路线已删）、FKA、真 VO、label 质量完整判据 | hosted NSAccessibility 树验证（需带 Accessibility authorization 的 signed runner） |
| ★ UI-16 VO 可操作契约（§22） | acc | release blocker，部分收敛：图像预览语义 label 已 Done source 侧（PR #35 `Image preview, W by H pixels` + stable identifier，master 32688965362）；settled generation 才播报已 Done（PR #48 settled search-count，`announceSettledSearchResultCount`，master 32743395877）；默认动作（Copy）有 PR #52 直测 + PR #41 skip-gated AXPress cell。差：NSAccessibility 树证据（CI hosted 树为空 `AXGroup`）、Apple VO 判据、真实 journey、FKA、localization | NSAccessibility 树 + Apple VO 判据 + 真实 journey（signed runner） |
| ~~Card 14 四 decision gate（04 L813-815）~~ | — | **已解决**（证据：账本 §21 Card 14A/14D 行 PR #36 master 32692472789、Batch 42 行 PR #47、Batch 43 行 PR #48、Batch 45 行 PR #50）。四 gate 均已有 chosen behavior + 对应 Reds/journeys：open 选 newest + search focus、close 清 query 由 PR #36 running-app tracer 钉死；快捷键冲突 chosen = 可见 + 可重试 + 失败保留旧绑定（`SummonShortcutController.swift` 契约注释 + PR #47 冲突警告测试 + PR #48 Settings 可见恢复 + PR #50 录制）；session/sleep chosen = 暂停 observation/admission 但不清除 willSleep 前已 admission 的值（`AppComposition.swift` 契约注释 + PR #47 workspace lifecycle hosted 证据）。决策以代码契约注释 + 测试钉死，无独立决策文档；物理 sleep/FUS/Secure Input/布局 runtime 归 §4.3 矩阵行 | — |
| ~~Card 14A/14C/14D（04）~~ | — | **已解决**（证据：账本 §21 "Card 14A/14D + first running-app XCUI tracer" 行 PR #36 master 32692472789、Batch 40 行 PRs #42–#44 master 32715020428、Batch 42 行 PR #47、Batch 43 行 PR #48）。14A：`HistoryPanelSurfaceState` 拥有 open/newest/arrow/close session generation，Return IME 委托 = `hasMarkedText == false` 门（PR #36）、Esc IME-safe（PR #48）；14C：hosted focus-loss/modal/reopen 生命周期（PRs #42–#44）+ workspace focus/power/login-session/screen 变化 hosted notification 证据（PR #47）；14D：AppDelegate 唯一 activate/deactivate owner（PR #36）。上限：hosted notification 证据 ≠ 物理 sleep/FUS/WindowServer；真实 IME 组合过程、真 Carbon delivery、signed runtime 仍不证（归 §4.3 矩阵行） | — |
| ~~自定义快捷键 + 冲突恢复（§22）~~ | — | **已解决**（证据：账本 §21 Batch 42/43/45 行——PR #47 controller 切片：安全 swap/Retry/reset/persistence/冲突警告测试；PR #48 所有权接线 + Settings 可见恢复，master 32743395877；PR #50 经既有安全注册事务的录制，master 32757784066。"固定 ⇧⌘C/⌃Space 且注册失败被忽略"的现状已消除）。残余：signed Carbon 布局/Secure Input runtime 矩阵仍 Open（账本 Batch 45 上限行；由 §4.3 "Carbon/快捷键/Secure Input 矩阵"行追踪，不在本节） | — |
| Localization 基建（§22） | acc | ~~String Catalog/FormatStyle/plurals 未接入~~ 部分接入：plurals 经 en.lproj `Localizable.stringsdict`（`%lld results` one/other）+ `"%lld+ results"` 落地，locale-aware 数字 FormatStyle 经 `LocalizedCountPresentation` 落地（PR #50 master 32757784066）。差：String Catalog（.xcstrings）未采用（现为 en.lproj `.strings`/`.stringsdict` 单语资源、仅 1 可见条目）；PR #51 新增 Appearance/Privacy 等 UI 文案不在本地化面；伪本地化/RTL 未证（PR #51 行明示"不证明超出复数计数的 localization"） | UI-17/P6-93a |
| ~~UI-7 键盘首路径契约（无行）~~ | — | **已解决**（证据：账本 §21 Card 14A/14D 行 PR #36 master 32692472789、§4.4 行 PR #41 master 32705436579、Batch 43 行 PR #48）。running-app XCUI journeys 已钉死契约：summon 选 newest、panel-open 无 click 输入即搜索、Down/Up 方向键、Return 穿 AppKit window owner 进同一 paste intent 后 panel 关闭（PR #36）；Clear 后重获 search focus（PR #41）；Esc IME-safe（PR #48）；`@FocusState isSearchFieldFocused` 已设（`HistoryPanelView.swift`/`SearchHeaderView.swift`）——原"FocusState 未设"过时。上限：IME 优先以 marked-text 门测试而非真实 CJK 组合过程（PR #36 行明确不证）；物理 IME/Carbon/signed runtime 归 §4.3 矩阵行 | — |
| ~~TODO-X7-1（§2.2）~~ | — | **已解决**（证据：账本 §21 "TODO-X7-1 App Intent entity fidelity" 行 = Done，Batch 40 source/tests + V2-05 update，PRs #42–#44，master 32715020428。purpose-specific wrapper DTO 携带 same-gated-projection authoritative Effective title/revision count；recent bounded page-ID scalar fetch；App Intent 移除 `0`/`Clipboard Item` 硬编码；BLOCKED-SPEC 已解除。上限：不新增 EntityQuery/second read/registry，仍不证 Siri/Shortcuts runtime、client custody、transport、opaque cursor） | — |

§4.5 的所有九行现在已与账本 (§21/§22)、git history、PR bodies 和当前源代码完成验证。替换文本如下。

### 4.5 隐私·安全·pasteboard 语义（9 项 · 剩余缺口 ~25%；1 关闭 / 6 部分 / 2 未动，2026-08-29 复核至 Batch 48/PR #54）〔区 C〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| Card 5A Pause UI（L298） | acc | 原"无可见限时 Pause/恢复入口"已多入口落地：footer 菜单 Pause/Resume + denied→allowed recovery journey（Batch 40，PRs #42–#44）；五分钟固定 Pause + 自动 privacy-preserving Resume（旧 deadline 不能结束新 pause，PR #46）；paused status-item 状态（PR #47）与状态栏右键 Pause/Resume（PR #51 `StatusItemMenu`→`AppDelegate.pauseCapture`）。账本仍记 Partial：只证 test privacy posture，real TCC prompt/revoke、systemDefault/ask/readFailure 完整 running 矩阵、signed install、FKA 未证 | 真实 TCC/privacy-posture running 矩阵 + signed/FKA（账本 §21 "CLIP-1 + visible Pause/Resume" 行上限） |
| CLIP-1 deny≠空历史（L298） | acc | ~~无区分性 UI~~ 已落地（Batch 40，PRs #42–#44）：empty+denied 替换普通 empty AX surface，retained rows 仍可 browse+banner；DEBUG exact denied/fixed 与 denied→allowed recovery journeys 钉死 no-capture/recheck/restart。余下与 Card 5A 同上限（test posture 之外的真实 TCC/prompt/revoke 矩阵、signed、FKA） | 随账本 §21 CLIP-1 行的剩余上限一并关闭 |
| ~~DEC-CAPTURE-OVERLOAD 判别实验~~（无行） | — | **已解决**（裁决 active+replaceable-latest：03 §13 DEC 注册表 RESOLVED 2026-08-24；spec 已入 01-architecture L201 / 06-cross-cutting L67 / 03-target-direction L153；落地 PR #46——current-by-ID paste 语义 + active+latest capture overload + 确定性 1,000-value lane stress，master correctness 32724982826 绿；账本 §21 Batch 41 行。残留不在本行范围：pre-freeze acquisition/transient overlap/RSS 仍 OPEN，见注册表行内注明） | — |
| ~~DATA-7a 单写者租约（无行）~~ | — | **已解决**（Batch 49，PR #58：`StoreRootLease`——StoreRoot 内独立零字节 `<store>.lease` artifact 上的 `fcntl(F_SETLK)` 整文件写锁（永不锁 SQLite 主文件、artifact 创建后永不 unlink），`ModelContainer` 创建前非阻塞获取、facade 寿命持有、任意进程退出由内核释放（无 stale 回收启发式——PLAY-DISK-0B "退出/崩溃后 fresh child 可重取" 由构造成立）；竞争抛新 public typed `HistoryFailure.persistence(.storeAlreadyOpen)`（03b §10 已同步，与 DATA-14 扁平 `.openStore` 区分），其余获取失败维持 `.openStore`；fcntl 每进程语义保留同进程重取（WS14/WS21 既有测试依赖，单测钉死），同进程无二写者仍由 composition root 静态 reservation 担保；三进程 child 证明（owner parked on stdin → 第二进程 `openRejectLeasedStore` typed 失败 → clean exit 释放 → fresh child 重取），correctness run [33689990278](https://github.com/GuangDai/Clipy/actions/runs/33689990278) 绿。机制出处：01-findings DATA-7 L203-213 已裁决"nonblocking、进程寿命内持有的窄 lease"） | — |
| DATA-9a volatile-UTI fixtures（无行） | acc | 部分落地：六个命名 volatile pasteboard UTI characterization（PR #46，不发明 ignore policy）+ 合成 unknown-UTI 字节敏感性 characterization（PR #50）。账本 Batch 45 明确"DATA-9a 仍 Partial（合成标签不证明真实 producer 频率）"：真实 Chrome/Safari/Notes fixture 频率证据仍缺 | 真实/授权 producer fixture 测频 |
| APL-C-05 措辞 + 密码管理器矩阵（无行） | acc | ~~措辞改写~~ 已完成：`PasteboardMarkers.swift:20` 改为 "six third-party convention strings as a best-effort denylist … None … is a framework-owned guarantee"（commit `5e77905`，Batch 40/PRs #42–#44）。差真实矩阵：逐版本密码管理器真实复制矩阵未跑（账本 Batch 40 ceiling："不证真实 browser/password-manager UTI"） | 真实密码管理器复制矩阵 |
| DATA-0 entitlement 负门（部分无行） | acc | 部分落地：hosted 运行时负门直测（PR #52/Batch 46：SecTask 运行时七键逐键 nil + SecCode 内嵌签名 `icloud-*`/`ubiquity-*` 前缀扫描，未签名宿主文档化保守通过；master correctness 33182041623 绿）。差 CI 门级化：CI lane `CODE_SIGNING_ALLOWED=NO` 下 entitlements 不嵌入签名，负门牙齿只在签名构建，manifest 侧仍是 review 义务（账本 Batch 46 ceiling 原文） | manifest 负门入常规 CI（或签名构建负门） |
| CLIP-7b / DEC-MULTI-ITEM | fut | 多项值图整链（顺序入 equality/fingerprint 等）（2026-08-29 核对：03 §13 注册表仍 OPEN（PB-MULTI），Batch 41–48 无新证据，无阈值实验启动） | 用户任务实验达阈值后设计 |
| DATA-9b 角色拆分 | fut | "可粘贴但去重忽略"的表示角色拆分（前置未变：9a 仍 Partial——合成标签不证明真实 producer 频率，"9a 证据确认"门槛未达成） | 9a 证据确认后判别测试 |

### 4.6 证据协议与开放问题（13 项 · ~40%）〔区 H/GOV〕

> 2026-08-29 更新注记：本节多数行以 "gate/scanner 入常规 CI" 为关闭路径，该路径已被
> AGENTS.md 2026-08-24 用户 policy 禁止（"不新增 policy locks/ratchets、rules/scanners、
> gates、ledgers/snapshots 等"；CI 刻意只保留 SwiftPM + generated-app 两条 correctness
> lane）。相关行标 〔policy-barred〕；机制类诉求若其载体已被删除则整行划线。

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ~~★ GOV-1 残留~~（无行） | — | **已解决**（证据：PR #34 / merge `f48d87f` / manual dispatch run [32685185124](https://github.com/GuangDai/Clipy/actions/runs/32685185124) attempt 2——same-SHA correctness 前置 + Exact A/B 与 5,000-row evidence 两 job 全绿，Exact schema v2 13 cases 全过且 `productionIntegrationEligible == true`；账本 §21 "GOV-1 current-green-SHA Exact A/B + 5,000-row evidence" 行） | — |
| ~~GOV-4 symbol snapshot 深锁~~（无行） | — | **已退役、不再成立**（证据：PR #41 / merge `87db3d6` 删除 `symbol-snapshot.yml`、`release-surface.yml`、`.swiftlint.yml`、`scripts/run_gates.sh`、`scripts/ci/run_source_gates.sh`、`scripts/public_symbol_snapshot.sh` 与 `SymbolSurface/*.symbols.txt` 全套；AGENTS.md 2026-08-24 明示 "no separate static-source or symbol-snapshot lane" 并禁重建）——policy-barred | — |
| GOV-4 三 gate（无行） | acc〔policy-barred（AGENTS.md 2026-08-24）〕 | 三个 gate（escape_hatch 扩 ClipyApp/Tests + 自测、import 依赖图门、"只有 HistoryAuthority 建 writable ModelContext" 所有权门）均未落地，且 gate/scanner 基建已随 PR #41 整体退役 | 禁新增 gate/rules/scanners；所有权关切只能走产品内手段（access-level 收缩先例：Batch 39 ThumbnailStore、Batch 48 PresentationUI 36 声明，账本 §21） |
| GOV-4/05-FC9 分类器对抗 fixtures | acc | 已换路线部分闭合：分类现状由产品侧子进程 characterization 钉死——PR #52（Batch 46 ①）future-schema（V5 fixture）与非 SQLite 字节两格、PR #53（Batch 47 ③）只读 0500 目录第三格，均经 `HistoryRestartProbe` 子进程穿公共 `SwiftDataHistory.open` 钉 `.persistence(.openStore)` 扁平（DATA-14；账本 §21）；"抽脚本 + fixtures 入 gate" 部分被 policy 禁止 | ENOSPC/transient-I/O/WAL/sidecar 维度归满盘 lane（PR #54 ② revise 满盘 cells 断言待 dispatch run）；macOS 27 `unknownDataStoreSchema` 可用前不引入 open 阶段分类器 |
| 05-FC7 RSS gate 正值（无行） | acc〔policy-barred（AGENTS.md 2026-08-24）〕 | 现状未变：dispatch admission lane 对 RSS 无正值要求——`finalize_evidence` 只查 `.time` 非空，`summarize_evidence` 缺值仍打 "unavailable" 不失败（`scripts/ci/run_performance_admission.sh`）；账本 GOV-1 行亦只把 RSS 记作 "进程 ceiling" | Card 17 gate 计划按 policy 为历史记录；如需收紧只能作为既有 evidence lane 自身合同维护的延伸（先例 commit `cea2b75`），不列为可领 gate 工作 |
| ~~05-FC10 symbol-bot 触发~~（无行） | — | **已退役、不再成立**（证据：PR #41 删除 symbol-snapshot workflow 与 bot 机制；`correctness.yml` 触发面只剩 master push/PR/dispatch，无 bot push 场景）——policy-barred（AGENTS.md 2026-08-24） | — |
| Card 17 gate 6/7/9（04） | acc〔policy-barred（AGENTS.md 2026-08-24）〕 | XcodeGen output 契约门、functional/perf 非零执行守卫、XcodeGen 固定版本+双生成 diff 三 gate 均未落地；playbook §22 执行卡 17 的 gate tracer 计划按 policy 属历史记录。现状：generated-app lane 真实构建并跑 ClipyApp 测试（无显式 executed>0 守卫） | 不作为可领工作（禁新增 gate）；XcodeGen 契约由 correctness lane 真实构建隐式覆盖 |
| PERF-9/05-FC8/FC4 诚实性能主张（无行） | acc | 大体未变：exact admission 仍按过时 ~125 s/request 估算定 11-sample profile（`Sources/HistoryPerfRunner/AdmissionProbes.swift` 注释），p95/p99 按 20/100-sample 支持下限诚实置 null；O(NlogN) 重标、render-speed 指标改名未做 | GOV-1 证据（PR #34，run 32685185124）已录 11-sample exact p50 2,666 ms 可作重定容依据；重标 + 恢复 p95/p99 仍待做 |
| ~~05-CE22 WS13 zero-publish 计数~~（L92 未含） | — | **已解决**（证据：PR #49 / merge `5cb3ec2` / 账本 §21 Batch 44 行"失败 History 事务 exact zero-publication 证明"——新增 `ConcurrencyHarness/SingleOperationInvalidationPublicationProbe` 非合并计数探针接入 `WS13TransactionFailureTests`，断言 `failedAttemptPublications.count == 0`） | — |
| SLO-1/SLO-2 指标集冻结（无行） | acc | 如旧：launch-to-ready/summon-to-row/keystroke-stable/selection-to-preview/Return-to-receipt/RSS 六指标未在 owning spec 冻结、无 arm64 硬件基线（全库 grep 仅评审文档提及）；GOV-1 dispatch lane 只覆盖非 UI 的 scale 指标 | 冻结 + 基线后批阈值（需 approved arm64 hardware，未变） |
| 05-ST1..6 统计纪律（无行） | acc | 部分已在 runner 强制：dispatch admission lane `finalize_evidence` 钉死 101/101/11 精确样本数，并要求 11-sample exact 的 p95/p99 为 null（20/100-sample 支持门；`af0a5a7` 2026-08-22 引入、`cea2b75` 2026-08-24 收紧完成合同）。冷热分开、超时计失败、比例覆盖尺度、无复合分仍无 runner 强制 | 剩余规则若入 runner 属既有 evidence lane 合同维护；Card 17 式新 gate 计划 policy-barred |
| 05 §7 33 项 Apple 开放问题 | acc | 已收窄 3 问：Q13 open 错误分类三格（future-schema/非 SQLite 字节/只读 0500）经 PR #52/#53 子进程 characterization 钉 `.openStore` 扁平（DATA-14）；Q9 migration/mid-transaction kill 原子性双窗口 probe 已就位于 PR #54（open，Batch 48 ①，待 CI 合并）；Q15 满盘 capture 叶已绿（§20 run 32636093920）、revise 满盘 cells 在 PR #54 ②（断言待 dispatch run），open/migration 与 remove/clear 满盘尾巴仍开（zero-demand 计划从不 admission-refuse）。Q27 经 PR #52 ④ external-clone verify 明确维持保守措辞（不断言 sidecar 存在） | 其余 30 问逐问 characterization cell 或保守措辞（未变） |
| 03 §13 DEC 注册表 36 OPEN + 2 BLOCKED | des | 注册表现为 44 行：13 RESOLVED + 29 OPEN + 2 BLOCKED-SPEC（本分支 committed 状态）。审计点名行已裁：DEC-RET-READ（PR #32）、DEC-PREVIEW-TARGET（PR #33）、DEC-CAPTURE-OVERLOAD 与 DEC-PASTE-REFERENCE（2026-08-24 裁决、Batch 41/PR #46 落地 active+replaceable-latest 与 current-by-ID）、DEC-OBSERVER-START（PR #50）、DEC-CAPTURE-CLOCK 与 DEC-RET-AGE（owning spec V2-02 已裁：DC-28 / §2.2 event-triggered，PR #50 冻结语义；03 §13 两行仍标 OPEN 属注册表滞后）、DEC-REVERT-RACE 与 DEC-UNPIN-SWEEP（PR #53，2026-08-28）。仍 OPEN：DEC-THUMB-CACHE（PR #54 只落测量叶供 G1 裁决）、DEC-DISTRIBUTION 等 | 逐行决策记录（余 29 OPEN；其中 2 行 owning spec 已裁、待注册表同步） |


依据核对说明（供合并者抽查）：GOV-1/CE22 的关闭直接对应账本 §21 行（PR #34 `f48d87f`、PR #49 `5cb3ec2`）；symbol-snapshot/gate 基建删除由 PR #41（merge `87db3d6`，commit `b964a6b` 删除文件清单经 `git show --stat` 核实）与 AGENTS.md 2026-08-24 policy 段共同证实；FC9/Q13/Q15/Q27 对应账本 §21 Batch 46（PR #52）、Batch 47（PR #53 `b8c3d56`）行；Q9 与 revise 满盘 cells 对应 Batch 48 行（PR #54 状态 OPEN、账本标 In progress——该两格未按"已解决"书写）；DEC 计数（44=13+29+2）由 `awk` 对 docs/reviews/2026-08-22-clipy-maccy-deep-review/03-target-direction.md §13 表实测，DEC-CAPTURE-CLOCK/DEC-RET-AGE 的 owning-spec 裁决见 docs/v2/V2-02-retention.md L567/L107（引入 commit `122c5ef` = Batch 45/PR #50）。目标文件：/lzcapp/document/Projects/ClipboardManager/docs/reviews/2026-08-22-clipy-maccy-deep-review/11-ai-todo-map-2026-08-23.md（L206–L222）。

以下为 `docs/reviews/2026-08-22-clipy-maccy-deep-review/11-ai-todo-map-2026-08-23.md` §4.7 的完整替换文本（已按账本 §21/§22、git log（origin/master=b8c3d56，PR #53）、`gh pr view 54`（state=OPEN，head=e7fc160=当前分支提交）、以及对 `Sources/PresentationUI`、`Sources/PasteboardAdapter`、`Sources/ContentPreview` 的逐符号 grep 核实；PR #54 未合并，账本 Batch 48 行记 **In progress**，故相关行按 In progress 引用，不虚报 Done）。

---

### 4.7 架构深化（7 项 · ~75%）〔GOV-2/3〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ~~★ GOV-2a / P0-0.3 `DEC-RET-READ`~~（“无处置行”标注已过时） | — | **已解决**（证据：PR #32、merge `1c221e6`、PR/master run 32678325377/32678654503、账本 §21 `DEC-RET-READ + Card 10A consumer` 行）。chosen 一句话：existing `ClipboardHistory.retentionConfiguration()` 裁决为 purpose-specific **public** configured-policy read（V2-02 §8.1a "resolved public"，不撤 API）；两个 Settings tabs 共享 one panel-owned snapshot/edit generation，Card 10A 消费面同批落地 | — |
| ~~★ GOV-2b / P0-0.4 `DEC-PREVIEW-TARGET`~~（“无处置行”标注已过时） | — | **已解决**（证据：PR #33、merge `ffd0e9f`、final/master run 32682438863/32682682345、账本 §21 `DEC-PREVIEW-TARGET / PLAY-PREVIEW-A1/A2` 与 `PLAY-PREVIEW-A3/A4/A5` 行、V2-PROGRESS 2026-08-24 Batch 31 行）。chosen 一句话：解码移入新 concrete package-only `ContentPreview` target（eager 中性 bounded raster artifact，PNG BGRA8/sRGB）；PresentationUI 禁 ImageIO 并删除 CGImage actor/public crossing——`ThumbnailStore.image(for:)`、`public enum PreviewContent`/`resolve`、`DisplayImageDecoder` 在当前 Sources 均零命中（本轮 grep 核实） | — |
| `DEC-THUMB-CACHE`（裁决处置行仍无） | acc | process-lifetime completed cache 仍是未准入机制：**裁决本身仍 OPEN**（03 §13 DEC 注册表 OPEN）。测量叶已落：Batch 48 / PR #54（分支已提交 `e7fc160`；账本 §21 Batch 48 行记 **In progress**、correctness CI 待绿）——DEBUG `ThumbnailMeasurement` sink + owner 七测 + 60-PNG 滚动 journey 以 XCTAttachment 导出重复请求率与 p50/p95，只断言采样完整性、无阈值 | 05-DE4 裁决待 G1 阈值判断（测量数据产出后可裁） |
| GOV-3 其余 knob 收缩 + surface gates | acc | 子项多数已闭合：① ThumbnailStore 公开 knobs/counters → Done（Batch 39 / PR #41 merge `87db3d6`、run 32705015919/32705436579：产品 init 固定 500/64MiB，custom limits/counters 降为 owner-test `package`）；② `PreviewContent.resolve` → 已随 PR #33 整体消失（Details 侧 `DetailsRepresentationPresentation.resolve` 自 PR #4 起即 `package`）；③ raw `NSPasteboard` public property → Batch 43 / PR #48（commit `4c0d7e4`、master 32743395877）收为 `internal`；④ `CaptureOutcome` payload init 已核实 `fileprivate`、非 caller 可构造（两轮审计基线即如此——时点审计此子项与代码不符）。剩余：⑤ PresentationUI 36 声明收缩已落 Batch 48 / PR #54（账本 In progress；3 个 HistoryStorage 协议 witness 因 public 协议一致性要求如实跳过）；~~ThumbnailStore 进一步收缩显式留给后续批~~ **ThumbnailStore 尾巴已收**（Batch 49，PR #58：`raster(for:)` 与 DEBUG `ThumbnailMeasurement.record(_:)` 收 `internal`——repo-wide grep 证明仅 in-module caller；产品缝维持 public 供 ClipyIntegrationTests（500/64MiB 固定 init 经新增 retention 钉测守望），owner-test 缝维持 `package`；测量叶行为不变——60-PNG journey 只经 env/JSONL 消费 sink，无编译期依赖） | “两 gate”路线 **policy-barred（AGENTS.md 2026-08-24：禁新增 gate/scanner；correctness CI 无 static-source/symbol-snapshot lane，Batch 43/47/48/49 均明记“无新 governance 机制/scanner/gate”）**——收缩以 per-PR per-symbol access 降级 + SRC/HOSTED 验证维持；ThumbnailStore 尾巴已由 Batch 49 收 |
| CaptureLane 提取 | fut | 删除测试证明 locality 改善才提取（2026-08-29 复核：gate 未变——账本 §22 明文维持，Batch 39 §4.7 行亦明示不关闭本项） | 证据先行 |
| ContentEditing 抽取 | fut | 出现第二个非 UI caller（Python edit）才深化（触发条件未达成：reviseContent PY-12A/B 仍 fut，见 §4.8） | 触发条件先行 |
| 05-ART12/DL17 序列化纪律（无行） | acc（gate 路线 policy-barred） | “artifact 只出自生产 owner 稳定 facts + 独立 pure serializer、测试不复制 UTI 表/路由”未制度化审计（2026-08-29 复核：仍无账本行、全 docs 无 ART12/DL17 处置记录；相关底座已存在——`ClipboardFormats` stable-facts 模块（PR #7）与各测试的独立 literal oracle——但纪律本身仍靠 per-PR 审评，未成文） | “入评审 gate”形式 **policy-barred（AGENTS.md 2026-08-24）**；纪律以 per-PR 审评维持；如需制度化只能走 spec/评审检查单，不得建 scanner/gate |

### 4.8 Python 本地自动化（18 项 · ~15%；0 关闭 / 18 未动，2026-08-29 复核至 Batch 48/PR #54——本节 Python 侧叶 Batch 21–48/PR #23–#54 均无处置行；Gateway 侧 F1 前置与认证内核的推进记于 §4.2，不关闭本节任何叶）〔区 I〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ★ clipyctl executable + process I/O（§22） | acc | X.8 只是 package-only pure codec；真实 FD/launch/ready/发现规则未交付 | 07 §2.1 CLI 骨架 + 发现规则冻结 |
| ★ DEC-PY-TRANSPORT（03 L810） | acc | UDS/Apple Events/App Intents/XPC+签名桥 的单一生产 transport 未选（F0A 只证 ad-hoc 同 EUID UDS 机制） | 07 §6 矩阵判定唯一 transport |
| PY-4 / B4/B5 signed 冷启 browse tracer（07 L671-679） | acc | 全退出 signed app + 仅 browsePreview 授权 + Python 拉起真 clipyctl→ready→三 locator 垂直 tracer | X.9 承接 |
| PY-5 browse 即敏感（07 L682-685） | acc | 合成 secret：无授权不出现在 response/audit/stderr；有授权只在 stdout 且 audit content-free | synthetic-secret grant 测试 |
| PY-7/8 Effective-only 有界二进制读（07 L694-707） | acc | 只返当前 Effective（禁 Canonical/修订/lineage）；cap±1/NUL/半帧/慢读/断开/继承 FD 逐卡 | PLAY-PY-D1A…D8 |
| PY-9A/9B organize + deleteItem（07 L710-716） | acc | 仅 pin/unpin 的 organize 与独立破坏性 grant 的 delete；确认政策 `DEC-PY-DELETE-UX` 未裁 | E1A/E1B；先裁 E2 |
| PLAY-PY-E5R/E5W（revoke race） | acc | park read 在 fast precheck 后、authoritative release 前 revoke 必须零输出；park write 在 commit 前 revoke 必须零写 | 各自独立 Red |
| PLAY-PY-E6A/E6B（timeout/no-blind-retry） | acc | CLI timeout 默认不 blind retry、返回 typed transient | 各自独立 Red |
| PY-13 同 UID 安全矩阵（07 L742-746） | acc | 同 UID Python/异 UID helper/错凭据/撤销后旧凭据：四负一格正 | PLAY-PY-B3A-C + B4；F3C |
| PY-15 四格 signed 发布矩阵（07 L754-759） | acc | 签名链/生命周期/调用方（Terminal·IDE·venv·launchd）/沙箱-TCC | F3A-D + aggregate |
| PLAY-PY-C1 纯投影 golden（无行） | acc | capability summary 纯投影/序列化 golden（可立即做） | golden fixtures |
| 05-ART1 能力快照（无行） | acc | LocalAutomationCapabilitySnapshot（build/沙箱/entitlement/transport/冷热/入口/peer 身份/typed failure） | 签名构建逐格取证 |
| reviseContent PY-12A/B | fut | 独立高危 grant + OCC + stale/fresh（评审排最后） | D9 后 E3/E4；两 DEC 先裁 |
| E7 持久幂等 + F2 + 订阅 | fut | `DEC-PY-IDEMPOTENCY` OPEN；F2 仅当需第二 adapter；订阅需溢出/重连/撤销语义 | 各 spec 批准后铸叶 |
| describeFormatCapabilities 端点 | fut | 被 `DEC-FORMAT-INVENTORY-OWNER`（BLOCKED-SPEC）挡 | 裁决后 PLAY-FORMAT-G |
| §5.2 沙箱/恶意同 UID 监护 | fut | 抵抗恶意同 EUID 或 App Sandbox 需新监护模型 | 触发时新决策 |

### 4.9 内容类型与预览（24 项 · 剩余缺口 ~20%；3 关闭 / 3 部分 / 其余未动，2026-08-29 复核至 Batch 48/PR #54/master `8b7486a`）〔区 J〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| Owner manifests + CapabilityInventory 九检查（§22） | acc | Search/Thumbnail/Preview/Edit/Pasteboard 各自 manifest 与 build/test inventory join（drift 门、HEIF/BMP 图标差、双 concealment 集共享） | TYPE-1/FORMAT-1D ratchet |
| ~~★ ContentPreview target + 四 spec 工件（08 L435-455）~~（“无行”标注已过时） | — | **已解决**（证据：PR #33、merge `ffd0e9f`、final/master run 32682438863/32682682345、账本 §21 `DEC-PREVIEW-TARGET / PLAY-PREVIEW-A1/A2` 行、AUDIT.md GOV-2b 行）。package-only `ContentPreview` target 已建并在产线（`Package.swift` target + `Sources/ContentPreview/ContentPreview.swift` + `Tests/ContentPreviewTests`，本轮核实）；spec 工件按 AUDIT GOV-2b 行落至 00 §2、01 §1/§2/§4/§5.7/§6、06 §5、roadmap README/05、V2-07 §11/§12，含 matching import gates（PresentationUI 源零 `import ImageIO`，本轮 grep）；仅 `.swiftlint.yml` 工件未以该形式落地（仓库无此文件，gate 以测试+评审形式存在） | — |
| ~~PREVIEW-1 loader→render seam tracer（无行）~~（“无行”标注已过时） | — | **已解决**（证据：PR #33、merge `ffd0e9f`、账本 §21 `PLAY-PREVIEW-A3/A4/A5 lifecycle` 行）。PLAY-PREVIEW-0 seam 随 ContentPreview target 建立（`ContentPreviewTests` 头注释自证 PLAY-PREVIEW-0/A1/A2）；DEBUG TaskLocal content-free hook 穿真实 `PreviewContentLoader → ContentPreview` seam，deterministic tests 覆盖 slow A→fast B（慢图→快切文本只发布后者）、panel close 与 same-ID v1→v2 late completion（关后无迟到发布）、active-job/retained-source-byte accounting 回零 | — |
| ~~DisplayImageDecoder CGImage 跨 actor（无行）~~（“无行”标注已过时） | — | **已解决**（证据：PR #33、merge `ffd0e9f`、账本 §21 `DEC-PREVIEW-TARGET` 行“PresentationUI禁ImageIO并删除CGImage actor/public state crossing”、AUDIT.md GOV-2b 已裁）。`DisplayImageDecoder` 在当前 Sources 零命中（本轮 grep，与 §4.7 GOV-2b 行核实一致）；PresentationUI 仅剩 `HistoryRowFiltering.loadIcon` 的 MainActor `CGImage?`（PR #51 源应用图标 seam，非内容解码产物） | — |
| 预览零外联 I/O 默认 + canary（无行） | acc | 默认 hover/dwell/缩略图零 DNS/HTTP/外文件/iCloud/promise 兑现/自动播放 + 临时文件清理 | ZERO-IO-DEFAULTS canary 全零 |
| FORMAT-2A HEIF/BMP 行图标 family（无行） | acc | public.heif/com.microsoft.bmp 可进缩略图/详情/预览但 HistoryRowView 图标缺 | family facts + 映射迁移 |
| FORMAT-2B 运行时准入交集 + PreviewCapabilitySnapshot（无行） | acc | 声明路由 ∩ 运行时能力 ∩ 实际解码 三元交集与 05-ART4 快照未建 | 单一 exact fixture + per-build 快照入 CI |
| FORMAT-3 unknown-UTI 往返门（无行） | acc | 合格未知/dyn.* UTI 的 raw 捕获/存储/逐字粘贴/无语义预览回归门。部分底座已落：Batch 40/45 合成 unknown/bookkeeping UTI 普通 capture-bytes characterization 与 ContentPreview `dyn.example`→`.unsupported` cell（PR #33/#50，账本 §21 明记“合成标签不证明真实 producer 频率”、DATA-9a 仍 Partial），但 PLAY-FORMAT-A1 三 fixture 逐字粘贴往返门仍未建 | PLAY-FORMAT-A1 三 fixture |
| UTF-16 字面量 + 表面分歧（无行） | acc | 硬编码 `public.utf8-external-plain-text` 是错误 Apple 字面量（仍在 `ClipboardFormats/StableFormatFacts.swift` 与 `HistoryRowFiltering.swift`，本轮 grep）；Projector/大预览可解 UTF-16 而 Details/Revise 强制 UTF-8 分歧 | facts 改正确字面量 + 分歧 manifest 化 |
| PREVIEW-RTF/PDF/HTML/FILE 四卡（无行） | acc | RTF 惰性链接+附件预算、PDF 有界页、HTML 离线纯文本优先、file-URL 元数据优先+两步显式加载 | 逐格式铸叶 |
| PERF-8 尺寸契约 + 回退裁决（无行） | acc | max(w,h) 致另一轴超界（ThumbnailService 仍 `MaxPixelSize = max(width,height)`，本轮核实）；候选回退须先批（05 §14.5 现 fail-closed） | 双轴 aspect-fit 或改标量契约；DEC 先裁 |
| PERF-10 垃圾字节分类（无行） | acc | junk 字节声明 public.png 现报 `.corruptStoredValue`（ThumbnailService §16 映射仍在，且失败不 cache 滚动重解码，本轮核实；PR #33 只给预览侧 typed `.malformedRepresentation`，未触及 thumbnail 路径），误报存储损坏并滚动重解码 | fake-PNG fixture 锁定重分类（或保留 fail-closed+文案） |
| PERF-5 离主解码实证（无行） | acc | 首绘/主线程心跳证据仍缺。eager 物化半已落：PR #33 ContentPreview 显式 `kCGImageSourceShouldCacheImmediately: true` 并在 off-actor detached task 画入 BGRA8 bitmap（账本 §21 “eager BGRA8/sRGB artifact”），旧 lazy 首帧路径随 `DisplayImageDecoder` 消失 | eager 物化 + 首绘/主线程心跳 |
| PERF-6 预览热路径（无行） | acc | 200ms dwell 触发 details 全量解码 canonical+修订且 Details 解码两次；dwell 政策未入产线 | bounded 前缀 + 非 main 解码一次 |
| 05-ART5 pasteboard 互操作 fixtures（无行） | acc | 多 item/重复 UTI/未知·动态·私有/空字节/延迟 provider/owner 变更 + 对象级读回 | fixture 集；真 App 矩阵其后 |
| 05-ART6 解码器语义 fixtures（无行） | acc | exact UTF-8/16、RTF/RTFD 附件、PNG/JPEG/HEIF/GIF/TIFF、PDF 各态、type-label mismatch。起步已落：PR #33 `ContentPreviewTests` 固定 exact UTF-8/native UTF-16/PNG 三族（迁移回归，账本 §21 明记“不扩大格式支持宣称”）；其余族仍缺 | 逐族 fixture |
| 05-ART7 解码器资源包络（无行） | acc | 巨尺寸/多帧/深元数据/截断/高压缩 的子进程 RSS/CPU/墙钟/像素/崩溃包络（ContentPreview 仅 64 MiB 聚合输入 admission，非子进程包络） | 逐格式子进程包络 |
| STEP-9 生产者/消费者矩阵（无行） | acc | TextEdit/Safari/Finder/截图/Preview/Photos/GIF/TIFF/PDF/自定义 provider 案例记录 | 逐案例入册 |
| HEIC/GIF/TIFF 逐格式 + UTF-16 编辑 + RTF 抽取 | fut | 各需 fixture/包络/错误映射/a11y | 08 §13 顺序 |
| WebKit/QuickLook/AV/URL·color·promise 路由 | fut | 各自 spec/证明门后启用（PR #51 的 Space quick-look overlay 是复用既有 preview seam 的 panel-local SwiftUI 层，非 QuickLook 框架路由启用，本轮核实） | 默认路径永不下放 |
| DEC-FORMAT-INVENTORY-OWNER + TIERS + 八维 | des | 能力投影 owner 依赖图/注入生命周期（BLOCKED-SPEC）、首版格式/包络阈值、八维模型 | spec/ADR 冻结 |
| MULTI-ITEM 六项 spec 裁决 | des | item 顺序入 equality/fingerprint、声明顺序持久 vs 排序、marker 拒整快照 vs 单项、lineage 多项写回、部分物化整败 vs 逐项、修订粒度 | 六项入 spec 后开 PB-MULTI-1B |
| CONTENTEDITING/编码 artifact/插件 seam | fut | 第二 caller/测量+spec/第二部署方 | 触发条件先行 |

### 4.10 分层存储与无上限历史（21 项 · ~10%；无整行关闭、3 项部分推进，2026-08-29 复核至 Batch 48/PR #54）〔区 D/K〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| CHAR-2/3 表征（无行） | acc | 近上限 canonical/修订 的 browse/details/paste/缩略图/search/R3 hydration 地图；缩略图整仓 reset/迟到完成/行增长曲线 | PLAY-STOR-2..4 receipts |
| TIER-1 四维预算（无行） | acc | logical/物理/cache/resident+in-flight 四维独立安全限与维度混填拒绝 | PLAY-MEM-1..7 |
| TIER-3 元数据-only 分页（无行） | acc | 一页 browse 0 内容字节 + scalar 行数≤limit+余量 | PLAY-STOR-2 + COUNT-7A（含 tie） |
| TIER-4 加权许可（无行） | acc | 源/工作区/输出分别计费、单请求超限 typed 拒绝、四释放路径、无 hold-and-wait、交互 paste 优先 | PLAY-MEM-* 逐叶 |
| TIER-5 内存压力响应（无行） | acc | warning 清冷未租 / critical 清可重建+暂停重预fetch；hosted DispatchSourceMemoryPressure 真实投递 | 注入+hosted 双 lane |
| TIER-6/PERF-7 ThumbnailStore 准入（无行） | acc | completed-only 500/64MiB 触发整仓 removeAll、迟到落逐出后、删除不精确修剪。2026-08-29 复核**仍开、部分推进**：per-surface 有界中间态持续在场（`Sources/PresentationUI/ThumbnailStore.swift` header 自述 "recorded intermediate state"、显式非 G1 共享 completed cache——completed fetch 才记录、任一界越界整仓 reset、request-token 迟到栅栏；Batch 39/PR #41 把产品 init 固定 500 entries/64MiB）；G1 裁决所需测量证据已落（Batch 48/PR #54 merge `8b7486a`：DEC-THUMB-CACHE 测量叶——DEBUG `ThumbnailMeasurement` sink + owner 七测 + 60-PNG 滚动 journey 以 XCTAttachment 导出重复请求率与 p50/p95、只断言采样完整性；账本 §21 Batch 48 行按 §23 规则仍记 In progress，master correctness 33225305486 截至刷新在跑）；**裁决本身仍 OPEN**（03 §13 `DEC-THUMB-CACHE` OPEN），共享 completed cache 获准与否及本行准入语义均未实现 | 05-DE4 裁决后实现（测量数据已产出，G1 可裁）；禁指纹盘身份 |
| ★ TIER-7 四个 O(N) 替换（无行） | acc | dedup 签名候选投影、捕获全量库存→事务聚合+受害者 cursor、search 全语料快照→索引候选+有界 top-K、策略扫全量解码→有界批 | PLAY-COUNT-1..5D 族（forced-collision 首捕仍合并） |
| TIER-8 UI 行窗口 + cursor 语义（无行） | acc | rows 持续 append 非可见窗口+lookahead；连续写下 cursor 无漏页/重复；pin 移动不重写全部 ordinal；验证不驻留全 ID 集 | COUNT-7A-7C/8A/8B（20k 行窗口不增长） |
| ★ DEC-U-SCALE-STARTUP-INDEX（blocked） | des | (a) P1 checkpoint 限 capped-profile + U-scale 换持久索引查询 或 (b) 修订 V2-06 P1——二选一未记录（前置于 PLAY-COUNT-1；禁双真相索引）。2026-08-29 复核：03 §13 注册表该行仍 OPEN | exclusive 决策记录 |
| TIER-9 开盖政策面（09 DESIGN-TIER-9 + §22 “tiered/unbounded production transition” 段；原 “§22 L339” 行号指针已随账本改版失效） | acc | count=nil 仅测试门；用户最大 unpinned 与全局硬上限双限修订、真实 5,001st、共享 PLAY-DISK 门+尺度/soak | TIER-7/8 绿后 |
| Card 2F / DATA-8 判别实验（无行） | acc | Storage hard content ceiling：先修容量可见性+测代表 store overhead，达阈值才转 TDD 卡 | 决策实验（验证器 B 补） |
| PLAY-DISK-3 cleanup priority（无行） | acc | 空间不足先删可重建 DerivedCache、committed item 仍可读；不自动删 oldest | Red→Green（验证器 B 补） |
| DATA-12/05-CE25 R3/回填 RSS（无行） | acc | N×8/64MiB 超 R3 阈值 sweep 与 V1→V2 大 blob 回填峰值 RSS/墙钟/reopen | Release-child 矩阵 |
| STORAGE-3 lineage hydration 成本（无行） | acc | 共享 hydrate 统一拉 Canonical+全修订：64/128/384MiB 边界峰值+settled RSS | 超预算才加 purpose-specific 读 |
| STORAGE-4/05-FC18 跨岛聚合准入 | acc | 同 key 合并/单飞/无界观察 Task 不构成聚合界；overlap-aware 资源门 | 自有 resident+in-flight 准入 |
| 四平面字节上报 + 维护页 + 触发矩阵（无行） | acc | logical/物理 store-family/derived cache/resident 分开（含高水位）；维护页标注不可用；时间/内存压力永不触发清扫 | REPORT-4BYTES/MAINT-DISPLAY/TRIGGER-MATRIX |
| 05-ART9/ART11/TS9 资源报告+容量/备份 fixture+soak（无行） | acc | TieredStorageResourceReport、APFS 容量/ENOSPC/备份含排除恢复 fixture、24-72h soak 曲线（capture 段 ENOSPC 今日已绿勿重做；open/revise 满盘 cells 已就位——PR #54 probe `openFullVolume`/`openSeededFullVolume`/`pressureRevise` 族 + `run_apfs_enospc.sh` 两轮满盘循环，断言只在 dispatch lane 产出、dispatch run 未归档；TieredStorageResourceReport/备份 fixture/soak 本体仍无行） | 逐 artifact |
| PLAY-TIER-1A/1B pre-G8 表征（无行） | acc | decoder access-mode characterization（具体 decoder/fixture/OS trace）与 pure access-plan。2026-08-29 复核：1A 要求的 header/range/full access-mode OS trace 仍无（PR #54 的 60-PNG 滚动 journey 只导出重复请求率/p50/p95，是 DEC-THUMB-CACHE G1 裁决证据、不替代 1A；1B 继续等 1A evidence profile 获批） | 04 §26 允许先做（2A-THUMB 已 Done） |
| P3 支线 TIER-10..17 + G8 | fut | blob depot/lease/GC/迁移/备份全线被 G8 admission 记录挡 | 无触发则整支跳过 |
| 50k→250k→1m 阶梯 + 诚实文案 | fut | 逐级独立子进程 soak；“无固定条数上限·受容量与安全保留约束” | 前级全过才开 |
| LANG-FREEZE / NO-GOD-CACHE / 备份单元 | des | 四平面术语冻结、禁中央混合缓存、禁称 .externalStorage 自动冷层、备份单元/安全删除边界 | spec 冻结 |

### 4.11 State-3 发布与签名（9 项 · ~10%；0 关闭 / 2 部分 / 7 未动，2026-08-29 复核至 Batch 48/PR #54）〔区 H〕

| ID | 层 | 差什么 | 怎么关 |
|---|---|---|---|
| ★ Developer ID 发布链（§22） | acc | 原“冻结 bundle ID/版本/构建/图标/entitlements 契约、protected ref archive”已有 source 契约（Card 16A，账本记 In progress）：`project.yml` 冻结 bundle `com.clipy.ClipyApp`、0.1.0/1、utility category、显式空 entitlements 与 AppIcon；manual-only [`release-archive.yml`](../../../.github/workflows/release-archive.yml) 先 same-SHA correctness，再要求 protected `release/<marketing>/<build>` tag + single exact checkout 并验证 unsigned ordinary `.xcarchive`，配 8 测 fail-closed validator（PR #36，merge `2bc4a8e`）。仍差：仓库无 protected release tag/ruleset（rulesets 为空）、无实际 archive run，全代码签名+Hardened Runtime+secure timestamp、notarize/staple/spctl、干净机复验+smoke 全部未动 | REL-1/P6-91 单链走完留证（Card 16A 下一步：protected tag/ruleset + 实际 archive run） |
| Signed state-3 证据 run（05-LAD3/EV7） | acc | 无签名 state-3 run 支撑 Gatekeeper/final entitlements/login item/TCC/升级；签名绝对 latency/RSS 缺（2026-08-29 核对：`signed-runtime` lane 仍 ad-hoc 签名，账本 §22 明令不得宣称 state 3） | state-3 run 归档 |
| App Sandbox 决策 + 迁移矩阵（P6-91-SANDBOX） | acc | 沙箱取舍未决；签名配置下 store 迁移/pasteboard/SMAppService 矩阵（DEC-DISTRIBUTION 注册表行仍 OPEN 且沙箱归其下，Batch 31–48 无新证据） | DEC-DISTRIBUTION 子决策 + 矩阵 |
| Entitlement 第三层门（05-CPD6/PD14） | acc | 第三层仍缺，但前两层之外已补 hosted 运行时负门直测：[`DataZeroEntitlementGateHostedTests`](../../../ClipyApp/Tests/ClipyIntegrationTests/DataZeroEntitlementGateHostedTests.swift)（SecTask 运行时七键逐键 nil + SecCode 内嵌签名 `icloud-*`/`ubiquity-*` 前缀扫描，未签名宿主文档化保守通过；PR #52/Batch 46，master correctness 33182041623 绿）。账本 ceiling 原文：CI lane `CODE_SIGNING_ALLOWED=NO` 下 entitlements 不嵌入签名，负门牙齿只在签名构建，manifest 侧仍是 review 义务——签名产物 entitlement 检查仍未入 release gate | release gate 加验证（签名构建或 manifest 级负门） |
| VO/FKA + String Catalog 同旅程发布门（P6-93） | acc | 所有控件稳定标识/标签/焦点序/键等价；VO 与 FKA 完成 summon-find-copy 同旅程 | 旅程完成判据 |
| DEC-DISTRIBUTION 裁决 | des | Developer ID 直发 vs MAS 的 spec 裁决（03 §13 注册表仍 OPEN，2026-08-29 核对无 ADR） | ADR 六字段 |
| 单一发行路径 scope | des | 先只 Developer ID 直发；禁同时 MAS/Homebrew/Sparkle | Phase-1 约束入 spec |
| Updater/MAS/Homebrew | fut | 未来 updater 的 feed/签名/artifact 出自同一发布事务 | 独立决策 |
| 导出功能 | fut | 需求/格式/授权三设计先行 | 获批后加入 |

## 5. 文档-账本冲突复核记录（验证器发现，领取前注意）

1. 07 §8.1/L837 称 audit barrier "未落地"，账本 X.6（L246）已 Done——**以账本为准**，仅
   crash-window 叶仍开。
2. GOV-1 的 Exact A/B 与 5k evidence job 是"被跳过的 dispatch job"，与 §20 两格无关，勿混。
3. 01 多条 acceptance 级发现无账本行（UI-2/UI-7/DATA-5a/10/7a/9a、CLIP-8/9、APL-C-05）——
   领取前必须先回 04 铸叶。
4. P0-0.3/P0-0.4（`DEC-RET-READ`/`DEC-PREVIEW-TARGET`）账本无处置行，而 unified-retention
   叶已在消费 readback seam——优先裁决。
5. 05-CE22 要求 WS13 附 zero-publish counter，账本 Done 行上限未列——docs 门更窄。
6. CLIP-2 要求"删除或收紧 public capture() convenience"，Card 5B Done 行未覆盖该收缩。
7. 09 TIER-12 要求每阶段 typed-fail；docs/05 §16 已接受 mid-transaction 耗尽为 crash
   ceiling——账本更窄且为已记录上限，blob-lane 各阶段仍须单独证。
8. V2 progress/roadmap 是否列全已落地集（X.4–X.8、F0A）无行可查——P0-0.2 V2 侧未证伪。
9. PERF-9/FC8/FC4 无行；账本 perf 行只声明"不证明性能"。
10. 05-FC6/GOV-4 snapshot 深锁、05-FC7 RSS 正值、05-FC10 bot 触发均无行。
11. DATA-0 的 manifest 级 entitlement 负门无行（只有 ad-hoc lane 负门 + 源码 `.none`）。
12. UI-2 的现状描述以 01 原文为准（评审时点）；领取时先复查当前实现再写 Red。

## 6. 今日（2026-08-23）已缩小清单（勿再计入缺口）

- Card 7/CLIP-1/X.7 的 "General Pasteboard 跨进程可见性" non-claim 移除（run 32632263996）。
- Card 6B 真实 APFS ENOSPC capture 叶转绿（run 32636093920）：真实竞争满盘下 §16
  admission 的 typed 拒绝、seed 不变、fresh-process reopen 全证。
- docs/05 §16 capacity admission（PR #25）：mid-transaction 耗尽从"未知"转为已记录接受
  的 Apple crash ceiling——调查负担转为诚实上限。
- APFS EXIT trap 定性 breadcrumbs-only（PR #27–#29）；instrumentation 行 Done。
- 账本 §7/§20、instrumentation、PROGRESS.md Batch 19 同步（PR #30）。
- 注意勿双计：Batch 21 五叶已有 branch source 未 merge——按 §1 规则尚非产品能力。

## 7. 建议领取顺序（2026-09-03 刷新至 Batch 49 / PR #58 后；与 00 §6 / 04 §4 的 gate 语义一致）

> 原第二批顺序（Batch 21 五叶 → P0 裁决 → …）已全部执行完毕并划线归档于 §4。
> 下表是剩余可领/待决项的当前排序；**2026-08-29 起按用户指示暂停新内容实现，
> 恢复实现工作时按此序领取。**

1. ~~**满盘 dispatch 证据归档**~~ → **Done（Batch 49）**：APFS run
   [33689980610](https://github.com/GuangDai/Clipy/actions/runs/33689980610) 绿，
   PR #54 的 revise/open 满盘 cells 断言归档（见 §4.3 两行）；顺带修两处 lane
   缺陷——capture fill cycle `filler` 清空后未重接管（run 33637018304 `of=""`）
   与 §16 admission URL 缓存陈旧（run 33687222086，真实产品缺陷，已修）。
2. ~~**GOV-3 尾巴**~~ → **Done（Batch 49）**：ThumbnailStore 尾巴已收（§4.7 ⑤）。
3. **DEC-THUMB-CACHE G1 裁决**：读 PR #54 journey 导出的重复请求率与 p50/p95
   （ci-result-bundles artifact），按 docs/06 §3 G1 双阈判据裁决 completed-cache
   准入与否，记录进 V2-07。
4. **unified retention 收尾**：localization（String Catalog 扩展到 retention 文案）
   + launch-at-login 四态 running 矩阵（§4.1 两行 partial；Card 10 的
   loosen/equal/文案兜底部分已由 Batch 49 关闭）。
5. **X.9 链剩余**：~~F1 client 侧 custody 文件~~（Batch 49 已关）→
   `DEC-PY-AUTHENTICATED-INGRESS` 解冻后的薄 ingress facade → clipyctl/transport
   （§4.2/§4.8；与签名证据耦合的部分仍 ci-impossible）。
6. **区 J/K characterization**：PLAY-STOR-2..4、PLAY-TIER-1A 等在各自决策门内
   可先行的表征叶（§4.9/§4.10）。
7. UI-16 真机 VO/FKA、Card 5A signed 矩阵、State-3 发行链——按 AGENTS.md 政策
   均须用户显式要求才启动，不自行领取。

## 8. Apple API 官方文档核实记录（两轮累计）

第一轮表（`supportedModes`/`IntentModes`/`AppDependencyManager`/`TransientAppEntity`/
`NSWindow.CollectionBehavior` 五旗标/`SMAppService`）继续有效，见 git 历史（本版未
删改其结论）。本轮新增：

| API | 核实结论 | 依据 |
|---|---|---|
| `URLResourceValues.volumeAvailableCapacityForImportantUsage` | 仅启动卷维护 purgeable 会计；**挂载/非启动卷实测返回 0**（run 32634051113），不可作 admission 事实 | Apple "Checking Volume Storage Capacity" 语义 + SO 56474397 同案 + 真实 run 证据 |
| `URLResourceValues.volumeAvailableCapacity` | 原始可用字节（与 df 同源）；全卷有效，admission 采用 | 同上 + run 32635233048 起实证 |
| Core Data external-storage interim file | 满盘创建失败抛 `NSInternalInconsistencyException`（非 NSError）→ 进程终止；throw site `+[_PFRoutines writePFExternalReferenceDataToInterimFile:]` | run 32632262141 保留 stderr（无官方文档——正是 05 memo UNKNOWN 预言的空白） |

## 9. 本审计的证据上限与复验记录

- 未在本机运行任何 Swift 构建/测试（Linux host）；"绿色"全部引用真实 GitHub run。
- **本版差距盘点方法**：13 个并行读手（00+README/01/03/04/05/06/07/08/09/账本 10）+
  合成 + 2 个对抗验证器（ledger 准确性 / 文档完备性）的 workflow；其中 04 与 06 的
  读手中途被 API 限流，其内容由验证器 B 直接以 04 原文补齐 11 条修正（Card 8C、
  11C/PERF-3、Card 14 族、2B-2、Card 17 gate 6/7/9、PLAY-PY E5R/E5W/E6A/E6B、
  2F/DATA-8、PLAY-DISK-3、PLAY-TIER-1A、3D 错挂修正），并由生成 agent 以账本 §21/§22
  与第一轮地图交叉核对；验证器 A 另补 2 条 Done-行 ceiling 尾巴（Card 6 banner
  物化、retention singleton 重启）。
- §2 的 grep 复核在 master `53c347d` 上执行（unmerged 零命中、`Task.sleep` 零命中、
  `ClipboardIntentModels` L70/81/86、`PasteboardMarkers.swift:20`、Settings
  `.success` ×4）。
- 完成度百分比为**粗估**（已关 leaf vs 本表聚合项口径），不是账本口径；引用以账本行为准。
- 04 卡片语义、03 DEC 表、05 盲区清单均以原文为准；本文件只做状态映射与新事实登记。
- 后续更新：请以下一个实现 PR 修正过时行；两格证据绿后，第一轮 §2.1/§2.4 中对应
  项已按本版 §2 复核关闭或保留。
