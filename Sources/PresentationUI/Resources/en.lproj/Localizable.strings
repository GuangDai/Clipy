/* Native .strings/.stringsdict tables resolve in both SwiftPM and Xcode
   builds (V2-07 §10). Retention Settings owns a separate RetentionSettings
   table in each supported localization. */

/* A lower-bound search result count; more results are available. */
"%lld+ results" = "%lld+ results";
