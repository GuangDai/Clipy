/// PresentationUI — view state and interactions over History DTOs. Owning spec:
/// docs/01-architecture.md §2 (target responsibilities). SwiftUI is confined to
/// this target but is deliberately not imported in the step-0 stub.

/// Step-0 scaffold placeholder; real surface lands per docs/roadmap README §3.
public enum PresentationUIScaffold {}
