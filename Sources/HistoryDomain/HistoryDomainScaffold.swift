/// HistoryDomain — the package-only functional core: content lineage, immutable
/// state, complete fact values, pure planners, and semantic mutation plans with
/// invariants. Owning spec: docs/02-domain.md (Part II); target role:
/// docs/01-architecture.md §2. Imports Foundation and HistoryCore only — no I/O,
/// actors, clocks, UUID generation, or persistence (docs/02-domain.md §1).
import Foundation

/// Step-0 scaffold placeholder; real surface lands per docs/roadmap README §3.
public enum HistoryDomainScaffold {}
