/// PasteboardAdapter — NSPasteboard observation/writes and translation to/from
/// HistoryCore raw values. Owning spec: docs/01-architecture.md §2 (target
/// responsibilities) and §4 (dependency classification and adapters). AppKit is
/// confined to this target but is deliberately not imported in the step-0 stub.

/// Step-0 scaffold placeholder; real surface lands per docs/roadmap README §3.
public enum PasteboardAdapterScaffold {}
