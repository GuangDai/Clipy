import Testing

/// Step-0 scaffold smoke test for the XcodeGen-hosted integration target.
///
/// At roadmap step 9b this target re-runs the WS1–WS21 paths end-to-end through the
/// composed app (real `SwiftDataHistory` + `PasteboardAdapter` + `PresentationUI`) —
/// the M3 re-verification, not a state-2 requirement (docs/roadmap/06-clipyapp.md
/// Acceptance; docs/roadmap/README.md §3).
@Test func scaffoldPlaceholder() {
    #expect(true)
}
